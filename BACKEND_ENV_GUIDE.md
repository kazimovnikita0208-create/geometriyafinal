# 📝 Настройка backend/.env файла

## 📋 Пример файла `backend/.env`

Создайте файл `backend/.env` на основе примера ниже:

```env
# ============================================
# Telegram Bot Configuration
# ============================================
BOT_TOKEN=your-telegram-bot-token-here
ADMIN_TELEGRAM_IDS=123456789,987654321

# ============================================
# JWT Authentication
# ============================================
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# ============================================
# Development Mode
# ============================================
# Установите в true для отключения авторизации в режиме разработки
DEV_MODE=false

# ============================================
# Database Configuration (SQLite)
# ============================================
# Путь к SQLite базе данных (по умолчанию: dev.db в корне backend)
DATABASE_URL=file:./dev.db

# ============================================
# Supabase Configuration (для миграции)
# ============================================
# URL вашего Supabase проекта
SUPABASE_URL=https://njcsizoiirqfsrzvlzec.supabase.co

# Service Role Key (секретный ключ с полным доступом)
# Получить: Supabase Dashboard → Settings → API → service_role key
# ⚠️ ВАЖНО: Никогда не коммитьте этот ключ в Git!
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ============================================
# Server Configuration
# ============================================
PORT=3001
NODE_ENV=development
```

## 🔑 Где взять значения

### 1. BOT_TOKEN
- Получите у [@BotFather](https://t.me/BotFather) в Telegram
- Или используйте существующий токен вашего бота

### 2. ADMIN_TELEGRAM_IDS
- Список Telegram ID администраторов через запятую
- Например: `123456789,987654321`
- Можно узнать свой ID через бота [@userinfobot](https://t.me/userinfobot)

### 3. JWT_SECRET
- Сгенерируйте случайную строку (минимум 32 символа)
- Можно использовать: `openssl rand -base64 32`
- Или любой другой безопасный случайный ключ

### 4. SUPABASE_URL
- Уже указан: `https://njcsizoiirqfsrzvlzec.supabase.co`
- Это URL вашего Supabase проекта

### 5. SUPABASE_SERVICE_ROLE_KEY
- Откройте Supabase Dashboard
- Settings → API
- Найдите **service_role key** (секретный ключ)
- Скопируйте и вставьте сюда

⚠️ **ВНИМАНИЕ:** Service Role Key имеет полный доступ к базе данных. Никогда не коммитьте его в Git!

## ✅ Минимальная конфигурация для миграции

Если вам нужно только запустить миграцию, достаточно:

```env
SUPABASE_URL=https://njcsizoiirqfsrzvlzec.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
```

## 🔒 Безопасность

1. ✅ Файл `.env` уже добавлен в `.gitignore`
2. ✅ Никогда не коммитьте `.env` в Git
3. ✅ Используйте разные ключи для development и production
4. ✅ Service Role Key - только для миграции, не используйте в production коде

## 📝 После создания файла

1. Сохраните файл как `backend/.env`
2. Заполните все необходимые значения
3. Запустите миграцию:
   ```bash
   cd backend
   node scripts/migrateToSupabase.js
   ```

---

**Готово! Теперь у вас есть правильный `.env` файл! 🎉**

