# 📅 Страница расписания - Современный дизайн

## 🎨 Что сделано:

### 1. **Единый стиль с главной страницей**
- ✅ `BeamsBackground` - анимированный фон с фиолетовыми лучами
- ✅ `Button` - оптимизированные кнопки с градиентами
- ✅ Фиолетовая цветовая палитра (#5833b6)
- ✅ Glass-morphism эффекты
- ✅ Современная типографика

---

## 📱 Компоненты и функционал:

### Header (Шапка)
```tsx
<div className="sticky top-0 z-20 bg-black/40 backdrop-blur-xl border-b border-purple-500/20">
```

**Особенности:**
- ✅ **Sticky** - прилипает к верху при скролле
- ✅ **Backdrop-blur** - размытие фона (легкое, не тормозит)
- ✅ Кнопка "Назад" с иконкой
- ✅ Заголовок + подзаголовок
- ✅ Адаптивные отступы (px-4 sm:px-6)

---

### Фильтры

**Дизайн:**
```tsx
bg-purple-900/40 backdrop-blur-xl rounded-2xl border border-purple-500/20
```

**2 фильтра:**
1. **Зал:**
   - Все залы
   - Волгина, 117А
   - Московское шоссе, 43

2. **Направление:**
   - Все направления
   - Pole Dance
   - Exotic Pole Dance
   - Растяжка
   - Воздушные полотна
   - Хореография

**Стилизация select:**
- ✅ Темный фон (`bg-black/40`)
- ✅ Фиолетовая граница
- ✅ Focus states (ring-2)
- ✅ Белый текст
- ✅ Плавные переходы

---

### Карточки занятий

**Структура каждой карточки:**

#### 1. **Header (верх)**
- Название занятия (Pole Dance, Растяжка и т.д.)
- Уровень в badge (Начинающие, Продолжающие)
- Количество свободных мест (3/8)

#### 2. **Info Grid (информация)**
Grid с 3 блоками:

**Время:**
- Иконка часов (ClockIcon)
- Дата: "Понедельник, 18 ноября"
- Время: "10:00 - 11:30"

**Адрес:**
- Иконка карты (MapPinIcon)
- "Адрес"
- Название зала: "Волгина, 117А"

**Инструктор:**
- Иконка людей (UsersIcon)
- "Инструктор"
- Имя: "Анна Иванова"

#### 3. **Actions (кнопки)**
- **"Записаться"** - `variant="default"` (яркая)
- **"Подробнее"** - `variant="outline"` (с границей)

**Hover эффект:**
```tsx
hover:border-purple-400/40
```
Граница становится светлее при наведении!

---

## 🎨 Цветовая схема:

### Фон:
- **BeamsBackground** - черный с фиолетовыми лучами
- **Карточки:** `bg-purple-900/40 backdrop-blur-xl`
- **Header:** `bg-black/40 backdrop-blur-xl`

### Текст:
- **Заголовки:** `text-white`
- **Обычный текст:** `text-purple-200`
- **Вторичный текст:** `text-purple-200/70`

### Границы:
- **Обычные:** `border-purple-500/20`
- **Hover:** `border-purple-400/40`
- **Focus:** `focus:border-purple-400`

### Badge (уровень):
- **Фон:** `bg-purple-500/20`
- **Текст:** `text-purple-200`

---

## 📊 Моковые данные:

Для демонстрации используются 3 занятия:

### Занятие 1:
```javascript
{
  title: 'Pole Dance',
  level: 'Начинающие',
  time: '10:00 - 11:30',
  date: 'Понедельник, 18 ноября',
  instructor: 'Анна Иванова',
  hall: 'Волгина, 117А',
  spots: 3,      // Свободно
  maxSpots: 8    // Всего
}
```

### Занятие 2:
```javascript
{
  title: 'Растяжка',
  level: 'Все уровни',
  time: '18:00 - 19:00',
  instructor: 'Мария Петрова',
  hall: 'Московское шоссе, 43',
  spots: 5,
  maxSpots: 10
}
```

### Занятие 3:
```javascript
{
  title: 'Exotic Pole Dance',
  level: 'Продолжающие',
  time: '19:30 - 21:00',
  instructor: 'Ольга Смирнова',
  hall: 'Волгина, 117А',
  spots: 1,      // Почти заполнено!
  maxSpots: 6
}
```

---

## 🚀 Интерактивность:

### State управление:
```tsx
const [selectedHall, setSelectedHall] = useState('all')
const [selectedDirection, setSelectedDirection] = useState('all')
```

**Готово к интеграции:**
- Фильтры сохраняют значения
- Легко подключить API для фильтрации
- Disabled state для кнопки при `spots === 0`

### Кнопки:

#### "Назад":
```tsx
<Button variant="ghost" onClick={() => router.back()}>
```

#### "Записаться":
```tsx
<Button 
  variant="default" 
  disabled={lesson.spots === 0}
>
  {lesson.spots === 0 ? 'Мест нет' : 'Записаться'}
</Button>
```

#### "Подробнее":
```tsx
<Button variant="outline">
  Подробнее
</Button>
```

---

## 📱 Адаптивный дизайн:

### Breakpoints:

#### Mobile (< 640px):
- Одна колонка для фильтров
- Одна колонка для info grid
- Компактные отступы (px-4, py-3)
- Меньший шрифт

#### Desktop (≥ 640px):
- Две колонки для фильтров
- Две колонки для info grid
- Просторные отступы (px-6, py-4)
- Крупный шрифт

### Responsive классы:
```tsx
// Отступы
px-4 sm:px-6
py-6 sm:py-8

// Шрифты
text-xl sm:text-2xl
text-lg sm:text-xl

// Grid
grid-cols-1 sm:grid-cols-2

// Высота
min-h-[120px] sm:min-h-[140px]
```

---

## 🎯 SVG Иконки:

### 4 иконки (вынесены наружу):

**ChevronLeftIcon** (стрелка назад):
```tsx
<ChevronLeftIcon />  // ← 
```

**ClockIcon** (часы):
```tsx
<ClockIcon />  // 🕐
```

**UsersIcon** (люди):
```tsx
<UsersIcon />  // 👥
```

**MapPinIcon** (локация):
```tsx
<MapPinIcon />  // 📍
```

**Почему вынесены наружу?**
- Создаются один раз (не при каждом рендере)
- Оптимизация производительности
- Лучше для React

---

## 💾 Структура файла:

```
schedule/page.tsx
├── Импорты (useState, useRouter, BeamsBackground, Button)
├── Иконки (4 компонента)
├── Моковые данные (mockLessons)
└── SchedulePage компонент
    ├── State (selectedHall, selectedDirection)
    ├── BeamsBackground
    │   ├── Header (sticky)
    │   └── Content
    │       ├── Фильтры
    │       ├── Список занятий
    │       └── Пустое состояние
```

---

## 🔧 Готовность к интеграции с API:

### Что нужно для подключения backend:

1. **Заменить mockLessons на API call:**
```tsx
const [lessons, setLessons] = useState([])

useEffect(() => {
  fetch('/api/lessons')
    .then(res => res.json())
    .then(data => setLessons(data))
}, [selectedHall, selectedDirection])
```

2. **Добавить loading state:**
```tsx
const [isLoading, setIsLoading] = useState(true)

{isLoading ? <LoadingSpinner /> : <LessonsList />}
```

3. **Обработчик записи:**
```tsx
const handleBookLesson = (lessonId: number) => {
  fetch(`/api/lessons/${lessonId}/book`, { method: 'POST' })
    .then(...)
}
```

4. **Фильтрация:**
```tsx
const filteredLessons = lessons.filter(lesson => {
  if (selectedHall !== 'all' && lesson.hall !== selectedHall) return false
  if (selectedDirection !== 'all' && lesson.direction !== selectedDirection) return false
  return true
})
```

---

## 🎨 Glass-morphism эффекты:

### Что делает интерфейс современным:

**backdrop-blur-xl:**
```tsx
backdrop-blur-xl
```
Размытие фона под элементами = эффект стекла

**Полупрозрачные фоны:**
```tsx
bg-purple-900/40  // 40% opacity
bg-black/40       // 40% opacity
```

**Тонкие границы:**
```tsx
border border-purple-500/20  // 20% opacity
```

**Результат:**
Элементы выглядят как полупрозрачное стекло на фоне с движущимися лучами! ✨

---

## ⚡ Производительность:

### Оптимизации:

1. **Иконки вынесены наружу** - создаются 1 раз
2. **Легкий backdrop-blur** - только где нужен
3. **Optimized transitions** - только необходимые свойства
4. **Правильные key** - `key={lesson.id}` (не index)
5. **Условный рендеринг** - `mockLessons.length === 0`

### Не тормозит:
- ✅ Плавный скролл
- ✅ Мгновенный отклик кнопок
- ✅ Плавные hover эффекты
- ✅ Быстрая смена фильтров

---

## 🎯 Пустое состояние:

Если занятий нет (`mockLessons.length === 0`):

```tsx
<div className="text-center py-12">
  <div className="text-6xl mb-4">📆</div>
  <h3 className="text-xl font-semibold text-white mb-2">
    Занятий не найдено
  </h3>
  <p className="text-purple-200/70 mb-6">
    Попробуйте изменить фильтры или вернитесь позже
  </p>
</div>
```

**UX:**
- Дружелюбное сообщение
- Подсказка что делать
- Эмодзи для эмоциональности

---

## 📋 Checklist:

- [x] BeamsBackground интегрирован
- [x] Button компонент используется
- [x] Фиолетовая цветовая схема
- [x] Glass-morphism эффекты
- [x] Адаптивный дизайн
- [x] SVG иконки оптимизированы
- [x] Фильтры с state
- [x] Карточки занятий с информацией
- [x] Кнопки "Записаться" и "Подробнее"
- [x] Пустое состояние
- [x] Sticky header
- [x] Hover эффекты
- [x] Disabled states
- [x] Производительность оптимизирована

---

## 🎉 Результат:

### Было (старая версия):
- ❌ Белый фон
- ❌ Обычные HTML элементы
- ❌ Простая типографика
- ❌ Нет карточек занятий
- ❌ Только заглушка

### Стало (новая версия):
- ✅ Анимированный фон с лучами
- ✅ Оптимизированные Button компоненты
- ✅ Современный glass-morphism
- ✅ Красивые карточки с информацией
- ✅ 3 примера занятий
- ✅ Функциональные фильтры
- ✅ Sticky header
- ✅ Адаптивный дизайн
- ✅ Единый стиль с главной страницей

---

**Страница готова! Современная, стильная, производительная! 💜⚡✨**

