# 🔍 Проверка миграций Supabase

## 📋 Шаг 1: Откройте Supabase Dashboard

1. Перейдите на https://supabase.com/dashboard
2. Выберите проект `njcsizoiirqfsrzvlzec`
3. Перейдите в **SQL Editor**

## ✅ Шаг 2: Проверка таблиц

Выполните этот SQL запрос для проверки всех таблиц:

```sql
-- Проверка всех таблиц
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public' 
ORDER BY table_name;
```

**Должны быть следующие таблицы:**
- users
- halls
- directions
- trainers
- lessons
- bookings
- subscriptions
- subscription_types
- notifications
- rental_bookings
- prices

## 📊 Шаг 3: Проверка данных

### Проверка количества записей:

```sql
-- Проверка количества записей в каждой таблице
SELECT 
  'users' as table_name, COUNT(*) as count FROM users
UNION ALL
SELECT 'halls', COUNT(*) FROM halls
UNION ALL
SELECT 'directions', COUNT(*) FROM directions
UNION ALL
SELECT 'trainers', COUNT(*) FROM trainers
UNION ALL
SELECT 'lessons', COUNT(*) FROM lessons
UNION ALL
SELECT 'bookings', COUNT(*) FROM bookings
UNION ALL
SELECT 'subscriptions', COUNT(*) FROM subscriptions
UNION ALL
SELECT 'subscription_types', COUNT(*) FROM subscription_types
UNION ALL
SELECT 'notifications', COUNT(*) FROM notifications
UNION ALL
SELECT 'rental_bookings', COUNT(*) FROM rental_bookings
UNION ALL
SELECT 'prices', COUNT(*) FROM prices;
```

## 🔧 Шаг 4: Если таблиц нет - выполните миграции

Если какие-то таблицы отсутствуют, выполните миграции в порядке:

### 4.1 Первая миграция (создание всех таблиц)

Откройте файл `supabase/migrations/001_initial_schema.sql` и выполните его в SQL Editor.

### 4.2 Вторая миграция (исправление identity columns)

Откройте файл `supabase/migrations/002_fix_identity_columns.sql` и выполните его.

### 4.3 Третья миграция (исправление day_of_week)

Откройте файл `supabase/migrations/003_fix_lessons_day_of_week.sql` и выполните его.

### 4.4 Четвертая миграция (удаление дубликатов)

Откройте файл `supabase/migrations/004_remove_duplicate_halls.sql` и выполните его.

## ⚠️ Важно

- Выполняйте миграции в правильном порядке
- Проверяйте результат после каждой миграции
- Если возникли ошибки, проверьте логи в Supabase Dashboard

---

**После выполнения всех миграций база данных будет готова к работе!**

