# 🔧 Исправление ошибки миграции

## Проблема

При миграции возникает ошибка: `cannot insert a non-DEFAULT value into column "id"`

Это происходит потому, что в PostgreSQL колонки с `GENERATED ALWAYS AS IDENTITY` не позволяют вставлять значения вручную.

## ✅ Решение

Нужно изменить схему на `GENERATED BY DEFAULT AS IDENTITY`, чтобы можно было вставлять значения.

### Вариант 1: Выполнить SQL скрипт исправления (Рекомендуется)

1. Откройте Supabase Dashboard → **SQL Editor**
2. Откройте файл `supabase/migrations/002_fix_identity_columns.sql`
3. Скопируйте весь SQL код
4. Вставьте в SQL Editor
5. Нажмите **Run**

### Вариант 2: Пересоздать таблицы

Если у вас еще нет данных в Supabase, можно:
1. Удалить все таблицы
2. Выполнить исправленный SQL скрипт `001_initial_schema.sql` (уже обновлен)

## После исправления

Запустите миграцию снова:

```bash
cd backend
.\migrate-to-supabase.bat
```

Теперь миграция должна пройти успешно! 🎉

