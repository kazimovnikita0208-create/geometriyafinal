# План разработки Telegram-бота с Mini App для студии «Геометрия»

## 📋 Обзор проекта

Telegram-бот со встроенным Mini App для студии танцев и растяжки «Геометрия» с двумя залами:
- Волгина 117А
- Московское шоссе 43, ТОЦ «Охотный ряд»

### Цветовая схема
- Основной: `#5833b6` (фиолетовый)
- Дополнительные: белый, чёрный
- Элементы дизайна: треугольники

---

## 🏗️ Архитектура системы

### Стек технологий

#### Backend
- **Runtime**: Node.js (v18+)
- **Framework**: Express.js
- **База данных**: SQLite3
- **ORM**: Better-SQLite3 или Prisma
- **Telegram Bot**: node-telegram-bot-api
- **Планировщик**: node-cron (для уведомлений)

#### Frontend (Mini App)
- **Framework**: Next.js 14+ (App Router)
- **Стилизация**: Tailwind CSS
- **UI компоненты**: Headless UI / shadcn/ui
- **Telegram WebApp**: @twa-dev/sdk
- **HTTP клиент**: Axios или Fetch API

#### Deployment
- **Backend**: VPS (например, Timeweb, Reg.ru) или Railway
- **Frontend**: Vercel или тот же VPS
- **База данных**: SQLite файл на сервере

---

## 🗄️ Структура базы данных

### Сущности (Таблицы)

#### 1. **Users** (Пользователи)
```sql
CREATE TABLE users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    telegram_id BIGINT UNIQUE NOT NULL,
    username TEXT,
    first_name TEXT,
    last_name TEXT,
    phone TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    is_active BOOLEAN DEFAULT 1,
    notifications_enabled BOOLEAN DEFAULT 1
);
```

**Назначение**: Хранение информации о пользователях, авторизованных через Telegram.

---

#### 2. **Halls** (Залы)
```sql
CREATE TABLE halls (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    address TEXT NOT NULL,
    capacity INTEGER DEFAULT 15,
    has_poles BOOLEAN DEFAULT 0,
    pole_count INTEGER DEFAULT 0,
    is_active BOOLEAN DEFAULT 1
);
```

**Назначение**: Информация о залах студии.

**Данные для заполнения**:
- Зал 1: Волгина 117А
- Зал 2: Московское шоссе 43, ТОЦ «Охотный ряд»

---

#### 3. **Directions** (Направления)
```sql
CREATE TABLE directions (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    description TEXT,
    color TEXT DEFAULT '#5833b6',
    icon TEXT,
    is_active BOOLEAN DEFAULT 1
);
```

**Назначение**: Направления танцев (например: Pole Dance, Растяжка, Exotic, Воздушные полотна и т.д.)

---

#### 4. **Subscription_Types** (Типы абонементов)
```sql
CREATE TABLE subscription_types (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    lesson_count INTEGER NOT NULL,
    validity_days INTEGER NOT NULL,
    price DECIMAL(10, 2) NOT NULL,
    description TEXT,
    is_active BOOLEAN DEFAULT 1
);
```

**Назначение**: Типы абонементов (4 занятия, 8 занятий, безлимит и т.д.)

**Примеры**:
- 4 занятия / 30 дней
- 8 занятий / 30 дней
- 12 занятий / 45 дней
- Безлимит / 30 дней

---

#### 5. **Subscriptions** (Абонементы пользователей)
```sql
CREATE TABLE subscriptions (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    subscription_type_id INTEGER NOT NULL,
    lessons_remaining INTEGER NOT NULL,
    valid_from DATE NOT NULL,
    valid_until DATE NOT NULL,
    is_active BOOLEAN DEFAULT 1,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (subscription_type_id) REFERENCES subscription_types(id)
);
```

**Назначение**: Активные абонементы пользователей.

---

#### 6. **Lessons** (Занятия в расписании)
```sql
CREATE TABLE lessons (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    hall_id INTEGER NOT NULL,
    direction_id INTEGER NOT NULL,
    teacher_name TEXT NOT NULL,
    start_time DATETIME NOT NULL,
    end_time DATETIME NOT NULL,
    capacity INTEGER DEFAULT 15,
    is_recurring BOOLEAN DEFAULT 0,
    recurrence_rule TEXT,
    description TEXT,
    is_active BOOLEAN DEFAULT 1,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (hall_id) REFERENCES halls(id),
    FOREIGN KEY (direction_id) REFERENCES directions(id)
);
```

**Назначение**: Расписание занятий (можно создавать разовые или повторяющиеся занятия).

**Примечание**: `recurrence_rule` может использовать формат RRULE (RFC 5545) для повторяющихся занятий.

---

#### 7. **Bookings** (Записи на занятия)
```sql
CREATE TABLE bookings (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    lesson_id INTEGER NOT NULL,
    subscription_id INTEGER NOT NULL,
    status TEXT DEFAULT 'confirmed',
    booked_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    cancelled_at DATETIME,
    cancellation_reason TEXT,
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (lesson_id) REFERENCES lessons(id),
    FOREIGN KEY (subscription_id) REFERENCES subscriptions(id)
);
```

**Назначение**: Записи пользователей на занятия.

**Статусы**: 
- `confirmed` - подтверждено
- `cancelled` - отменено
- `completed` - завершено
- `no_show` - не пришёл

---

#### 8. **Pole_Bookings** (Бронирование пилонов/зала)
```sql
CREATE TABLE pole_bookings (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    hall_id INTEGER NOT NULL,
    pole_number INTEGER,
    start_time DATETIME NOT NULL,
    end_time DATETIME NOT NULL,
    status TEXT DEFAULT 'confirmed',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (hall_id) REFERENCES halls(id)
);
```

**Назначение**: Бронирование пилонов или всего зала для индивидуальных тренировок.

---

#### 9. **Notifications** (Уведомления)
```sql
CREATE TABLE notifications (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER,
    type TEXT NOT NULL,
    title TEXT NOT NULL,
    message TEXT NOT NULL,
    is_sent BOOLEAN DEFAULT 0,
    sent_at DATETIME,
    scheduled_for DATETIME,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id)
);
```

**Назначение**: Очередь уведомлений для отправки через бота.

**Типы**:
- `lesson_reminder` - напоминание о занятии (за 2 часа)
- `promo` - рекламные акции
- `event` - мероприятия
- `subscription_expiring` - абонемент заканчивается

---

#### 10. **Settings** (Настройки системы)
```sql
CREATE TABLE settings (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    key TEXT UNIQUE NOT NULL,
    value TEXT NOT NULL,
    description TEXT,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

**Назначение**: Глобальные настройки (правила отмены, контактная информация и т.д.)

**Примеры настроек**:
- `cancel_evening_hours_before`: 4
- `cancel_morning_deadline_hour`: 21
- `morning_classes_start`: 10
- `morning_classes_end`: 15
- `reminder_hours_before`: 2

---

## 🔌 API Endpoints (Backend)

### Базовый URL: `/api/v1`

### 1. Авторизация
- `POST /auth/telegram` - Авторизация через Telegram WebApp InitData

### 2. Пользователи
- `GET /users/me` - Получить данные текущего пользователя
- `PUT /users/me` - Обновить профиль
- `PATCH /users/me/notifications` - Включить/выключить уведомления

### 3. Абонементы
- `GET /subscriptions/types` - Список типов абонементов
- `GET /subscriptions/my` - Мои активные абонементы
- `GET /subscriptions/:id` - Детали абонемента

### 4. Расписание и занятия
- `GET /lessons` - Получить расписание
  - Query params: `?hall_id=1&direction_id=2&date_from=2025-01-01&date_to=2025-01-31`
- `GET /lessons/:id` - Детали занятия
- `GET /lessons/:id/availability` - Проверить доступные места

### 5. Записи на занятия
- `POST /bookings` - Записаться на занятие
  - Body: `{ lesson_id, subscription_id }`
- `GET /bookings/my` - Мои записи (будущие)
- `DELETE /bookings/:id` - Отменить запись (с проверкой правил)
- `GET /bookings/:id/can-cancel` - Можно ли отменить запись

### 6. Бронирование зала/пилонов
- `GET /pole-bookings/availability` - Доступность пилонов
  - Query: `?hall_id=1&date=2025-01-15`
- `POST /pole-bookings` - Забронировать пилон/зал
- `GET /pole-bookings/my` - Мои бронирования
- `DELETE /pole-bookings/:id` - Отменить бронирование

### 7. Справочники
- `GET /halls` - Список залов
- `GET /directions` - Список направлений

### 8. Админ (опционально для MVP)
- `POST /admin/lessons` - Создать занятие
- `PUT /admin/lessons/:id` - Обновить занятие
- `DELETE /admin/lessons/:id` - Удалить занятие
- `GET /admin/bookings` - Все записи
- `POST /admin/notifications/broadcast` - Отправить рассылку

---

## 🎯 Основные сценарии использования

### 1. Авторизация пользователя
```
1. Пользователь открывает Mini App через кнопку в боте
2. Mini App получает initData от Telegram
3. Frontend отправляет initData на /api/v1/auth/telegram
4. Backend валидирует initData, создаёт/обновляет пользователя
5. Возвращает JWT токен для дальнейших запросов
6. Frontend сохраняет токен и показывает главный экран
```

### 2. Просмотр расписания и запись на занятие
```
1. Пользователь нажимает "Записаться"
2. Видит расписание с фильтрами (зал, направление, дата)
3. Выбирает занятие
4. Система показывает детали: время, преподаватель, свободные места
5. Пользователь выбирает абонемент (если их несколько)
6. POST /api/v1/bookings
7. Backend проверяет:
   - Есть ли активный абонемент
   - Есть ли занятия на абонементе
   - Не истёк ли абонемент
   - Есть ли свободные места
   - Не записан ли уже пользователь
8. Создаёт запись, уменьшает счётчик занятий
9. Frontend показывает подтверждение
10. Backend планирует напоминание за 2 часа
```

### 3. Отмена записи с проверкой правил
```
1. Пользователь открывает "Личный кабинет" → "Мои занятия"
2. Выбирает занятие для отмены
3. Frontend делает GET /api/v1/bookings/:id/can-cancel
4. Backend проверяет правила:
   
   ПРАВИЛО 1: Вечерние занятия (после 15:00)
   - Отмена возможна не позднее чем за 4 часа до начала
   - Пример: занятие в 19:00 → отменить можно до 15:00
   
   ПРАВИЛО 2: Дневные занятия (10:00-15:00)
   - Отмена возможна только до 21:00 предыдущего дня
   - Пример: занятие в среду 12:00 → отменить можно до вторника 21:00
   
5. Если можно отменить:
   - Показывается кнопка "Отменить запись"
   - При подтверждении → DELETE /api/v1/bookings/:id
   - Возвращается занятие на абонемент
   - Статус записи меняется на 'cancelled'
   
6. Если нельзя:
   - Показывается сообщение с причиной
   - Кнопка отмены неактивна
```

### 4. Личный кабинет
```
1. Пользователь нажимает "Личный кабинет"
2. GET /api/v1/subscriptions/my - получить абонементы
3. GET /api/v1/bookings/my - получить будущие занятия
4. Отображается:
   - Активные абонементы (тип, осталось занятий, срок действия)
   - Список будущих занятий с возможностью отмены
   - История посещений (опционально)
```

### 5. Отправка уведомлений (Backend cron job)
```
1. Каждые 5 минут запускается планировщик
2. Выбирает занятия, которые начнутся через 2 часа
3. Для каждой записи создаёт уведомление
4. Отправляет через Telegram Bot API
5. Отмечает уведомление как отправленное
```

### 6. Бронирование пилона/зала
```
1. Пользователь выбирает "Забронировать зал"
2. Выбирает зал, дату и время
3. GET /api/v1/pole-bookings/availability
4. Видит свободные слоты/пилоны
5. POST /api/v1/pole-bookings
6. Получает подтверждение
```

---

## 🔐 Безопасность

### Валидация Telegram WebApp InitData
```javascript
// Backend должен проверять подлинность данных от Telegram
// Используя секретный токен бота и HMAC-SHA256
function validateTelegramWebAppData(initData, botToken) {
  // Реализация согласно документации Telegram
}
```

### JWT токены
- После успешной авторизации выдавать JWT
- Срок действия: 30 дней
- Refresh token для продления

### Защита API
- Все эндпоинты (кроме /auth) требуют валидный JWT
- Rate limiting для предотвращения злоупотреблений

---

## 🎨 UI/UX Mini App

### Главный экран (Home)
```
┌─────────────────────────────┐
│  [Logo] Геометрия          │
│                             │
│  ┌─────────────────────┐   │
│  │  📅 Записаться      │   │
│  └─────────────────────┘   │
│                             │
│  ┌─────────────────────┐   │
│  │  💃 О направлениях  │   │
│  └─────────────────────┘   │
│                             │
│  ┌─────────────────────┐   │
│  │  💰 Цены            │   │
│  └─────────────────────┘   │
│                             │
│  ┌─────────────────────┐   │
│  │  👤 Личный кабинет  │   │
│  └─────────────────────┘   │
└─────────────────────────────┘
```

### Экран "Записаться" (Schedule)
- Фильтры: зал, направление, неделя
- Карточки занятий с информацией
- Индикатор свободных мест
- Цвет карточки зависит от направления

### Экран "Личный кабинет"
- Информация об абонементе
- Список будущих занятий
- Кнопки отмены с индикацией возможности

### Дизайн-система
- Цвета:
  - Primary: `#5833b6`
  - Background: `#ffffff`, `#000000`
  - Accent: треугольные паттерны
- Шрифты: Inter, Montserrat или SF Pro
- Компоненты: закруглённые углы, тени, градиенты

---

## 🤖 Telegram Bot

### Команды бота
- `/start` - Приветствие + кнопка "Открыть Mini App"
- `/help` - Помощь
- `/schedule` - Быстрый просмотр расписания (опционально)
- `/mylessons` - Мои занятия (опционально)

### Инлайн-кнопка
```javascript
bot.sendMessage(chatId, 'Добро пожаловать! 👋', {
  reply_markup: {
    inline_keyboard: [[
      { text: '📱 Открыть приложение', web_app: { url: 'https://your-mini-app-url.com' } }
    ]]
  }
});
```

### Уведомления
Бот отправляет:
- Напоминания о занятиях (за 2 часа)
- Уведомления об истечении абонемента (за 3 дня)
- Рассылки об акциях/мероприятиях

---

## 📦 Структура проекта

```
geometriya-bot/
├── backend/
│   ├── src/
│   │   ├── config/
│   │   │   ├── database.js
│   │   │   └── telegram.js
│   │   ├── models/
│   │   │   ├── User.js
│   │   │   ├── Subscription.js
│   │   │   ├── Lesson.js
│   │   │   ├── Booking.js
│   │   │   └── ...
│   │   ├── routes/
│   │   │   ├── auth.js
│   │   │   ├── subscriptions.js
│   │   │   ├── lessons.js
│   │   │   ├── bookings.js
│   │   │   └── ...
│   │   ├── controllers/
│   │   │   └── ...
│   │   ├── services/
│   │   │   ├── telegramAuth.js
│   │   │   ├── bookingRules.js
│   │   │   └── notificationScheduler.js
│   │   ├── middleware/
│   │   │   ├── auth.js
│   │   │   └── errorHandler.js
│   │   ├── bot/
│   │   │   ├── index.js
│   │   │   └── handlers.js
│   │   └── index.js
│   ├── database/
│   │   ├── schema.sql
│   │   └── geometriya.db
│   ├── package.json
│   └── .env
│
├── frontend/
│   ├── app/
│   │   ├── layout.tsx
│   │   ├── page.tsx
│   │   ├── schedule/
│   │   ├── profile/
│   │   ├── directions/
│   │   └── prices/
│   ├── components/
│   │   ├── ui/
│   │   ├── LessonCard.tsx
│   │   ├── SubscriptionCard.tsx
│   │   └── ...
│   ├── lib/
│   │   ├── api.ts
│   │   ├── telegram.ts
│   │   └── utils.ts
│   ├── styles/
│   │   └── globals.css
│   ├── public/
│   ├── package.json
│   ├── tailwind.config.js
│   └── next.config.js
│
└── README.md
```

---

## 🚀 План разработки (этапы)

### Этап 1: Подготовка (1-2 дня)
- [ ] Создать репозиторий Git
- [ ] Настроить структуру проекта (backend + frontend)
- [ ] Настроить окружение разработки
- [ ] Создать Telegram бота через BotFather
- [ ] Установить зависимости

### Этап 2: База данных (1 день)
- [ ] Создать схему SQLite базы данных
- [ ] Написать SQL скрипт создания таблиц
- [ ] Подготовить seed data (залы, направления, типы абонементов)
- [ ] Настроить подключение к БД в backend

### Этап 3: Backend API - Базовый функционал (3-4 дня)
- [ ] Настроить Express.js сервер
- [ ] Реализовать авторизацию через Telegram WebApp
- [ ] Создать JWT middleware
- [ ] Реализовать endpoints для:
  - [ ] Авторизация (`/auth/telegram`)
  - [ ] Пользователи (`/users/*`)
  - [ ] Залы и направления (`/halls`, `/directions`)
  - [ ] Абонементы (`/subscriptions/*`)

### Этап 4: Backend API - Расписание и записи (3-4 дня)
- [ ] Реализовать endpoints для расписания (`/lessons`)
- [ ] Реализовать логику записи на занятия (`/bookings`)
- [ ] Реализовать **правила отмены записей**:
  - [ ] Функция проверки возможности отмены
  - [ ] Вечерние занятия: за 4 часа
  - [ ] Дневные занятия: до 21:00 предыдущего дня
- [ ] Реализовать endpoints для бронирования пилонов
- [ ] Тестирование бизнес-логики

### Этап 5: Telegram Bot (2-3 дня)
- [ ] Настроить node-telegram-bot-api
- [ ] Реализовать команды (`/start`, `/help`)
- [ ] Добавить кнопку для открытия Mini App
- [ ] Реализовать систему уведомлений:
  - [ ] Функция отправки сообщений
  - [ ] Cron job для напоминаний о занятиях
  - [ ] Cron job для уведомлений об истечении абонемента
- [ ] Реализовать функцию массовой рассылки

### Этап 6: Frontend Mini App - UI Kit (2-3 дня)
- [ ] Настроить Next.js с App Router
- [ ] Настроить Tailwind CSS с фирменными цветами
- [ ] Создать дизайн-систему (кнопки, карточки, инпуты)
- [ ] Интегрировать @twa-dev/sdk
- [ ] Создать Layout с навигацией
- [ ] Реализовать компоненты:
  - [ ] Button (с треугольными паттернами)
  - [ ] Card
  - [ ] LoadingSpinner
  - [ ] Modal

### Этап 7: Frontend - Главный экран и авторизация (2 дня)
- [ ] Реализовать главный экран с 4 кнопками
- [ ] Настроить API клиент (axios/fetch)
- [ ] Реализовать авторизацию через initData
- [ ] Сохранение JWT токена
- [ ] Защищённые роуты

### Этап 8: Frontend - Расписание и запись (3-4 дня)
- [ ] Страница расписания (`/schedule`)
- [ ] Фильтры по залу и направлению
- [ ] Компонент LessonCard
- [ ] Модальное окно с деталями занятия
- [ ] Форма записи на занятие
- [ ] Обработка ошибок (нет абонемента, нет мест и т.д.)

### Этап 9: Frontend - Личный кабинет (2-3 дня)
- [ ] Страница профиля (`/profile`)
- [ ] Отображение активных абонементов
- [ ] Список будущих занятий
- [ ] Кнопка отмены с проверкой правил
- [ ] Модальное окно подтверждения отмены
- [ ] Сообщения о причинах невозможности отмены

### Этап 10: Frontend - Дополнительные страницы (2 дня)
- [ ] Страница "О направлениях" (`/directions`)
- [ ] Страница "Цены" (`/prices`)
- [ ] Страница бронирования зала/пилонов
- [ ] Адаптивный дизайн для всех страниц

### Этап 11: Тестирование и отладка (3-4 дня)
- [ ] End-to-end тестирование всех сценариев
- [ ] Тестирование правил отмены
- [ ] Тестирование уведомлений
- [ ] Проверка работы на мобильных устройствах
- [ ] Исправление багов
- [ ] Оптимизация производительности

### Этап 12: Деплой (1-2 дня)
- [ ] Настроить VPS/хостинг
- [ ] Деплой backend на сервер
- [ ] Деплой frontend на Vercel
- [ ] Настроить домен для Mini App
- [ ] Настроить HTTPS
- [ ] Настроить автозапуск бота
- [ ] Финальное тестирование в продакшене

### Этап 13: Документация и передача (1 день)
- [ ] Написать README с инструкциями
- [ ] Документация API
- [ ] Инструкция для администратора
- [ ] Инструкция по добавлению расписания
- [ ] Обучение владельца студии

---

## ⏱️ Оценка времени

**Общее время разработки**: 25-35 дней (1-1.5 месяца)

- При работе одного разработчика full-time: 5-6 недель
- При работе в команде (backend + frontend): 3-4 недели

---

## 🔧 Дополнительные улучшения (после MVP)

### Фаза 2 (опционально)
- [ ] Админ-панель для управления расписанием
- [ ] История посещений
- [ ] Система отзывов и рейтингов
- [ ] Push-уведомления (через Telegram)
- [ ] Интеграция с платёжными системами
- [ ] QR-коды для отметки посещения
- [ ] Статистика для администратора
- [ ] Экспорт данных в Excel
- [ ] Резервное копирование БД
- [ ] Миграция на PostgreSQL (если нужна масштабируемость)

---

## 📝 Важные замечания

### Правила отмены записей
Реализовать строгую проверку как на backend, так и на frontend:

```javascript
// Псевдокод для проверки
function canCancelBooking(lesson, bookingTime) {
  const lessonStart = new Date(lesson.start_time);
  const now = new Date();
  const lessonHour = lessonStart.getHours();
  
  // Вечерние занятия (после 15:00)
  if (lessonHour >= 15) {
    const hoursUntilLesson = (lessonStart - now) / (1000 * 60 * 60);
    return hoursUntilLesson >= 4;
  }
  
  // Дневные занятия (10:00-15:00)
  if (lessonHour >= 10 && lessonHour < 15) {
    const previousDay = new Date(lessonStart);
    previousDay.setDate(previousDay.getDate() - 1);
    previousDay.setHours(21, 0, 0, 0);
    return now <= previousDay;
  }
  
  return false;
}
```

### Безопасность
- Всегда валидировать initData от Telegram
- Использовать HTTPS для Mini App
- Не хранить чувствительные данные в localStorage
- Логировать важные действия (запись, отмена)

### Производительность
- Кэшировать справочники (залы, направления)
- Использовать индексы в SQLite
- Оптимизировать запросы расписания

---

## 📞 Контакты и поддержка

После завершения проекта предусмотреть:
- Техническую поддержку
- Обучение администраторов
- Резервное копирование данных
- Мониторинг работы бота

---

## 🎉 Результат

Telegram-бот с полнофункциональным Mini App, который позволит студии «Геометрия»:
- Автоматизировать запись на занятия
- Управлять абонементами клиентов
- Отправлять уведомления и рассылки
- Снизить нагрузку на администраторов
- Улучшить клиентский опыт

**Стек**: Node.js + Express + SQLite + Next.js + Telegram Bot API + Tailwind CSS

