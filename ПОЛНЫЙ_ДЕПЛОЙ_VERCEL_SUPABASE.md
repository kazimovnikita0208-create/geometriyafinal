# 🚀 Полный деплой проекта на Vercel с Supabase

## 📋 Обзор

Этот документ описывает полный процесс деплоя проекта на Vercel с подключением к Supabase для работы с базой данных.

## ✅ Что уже готово

1. ✅ Frontend настроен для работы с Supabase
2. ✅ Backend имеет адаптер для работы с Supabase
3. ✅ Миграции Supabase выполнены
4. ✅ Данные мигрированы в Supabase

## 🔧 Шаг 1: Настройка переменных окружения в Vercel

### Для Frontend проекта

1. Откройте Vercel Dashboard: https://vercel.com/dashboard
2. Выберите проект `geometriyafinal`
3. Перейдите в **Settings** → **Environment Variables**
4. Добавьте следующие переменные:

#### Обязательные переменные для Frontend:

```env
# API Backend URL (после деплоя backend)
NEXT_PUBLIC_API_URL=https://ваш-backend.vercel.app

# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://njcsizoiirqfsrzvlzec.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im5qY3Npem9paXJxZnNyenZsemVjIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjUwMDg2OTUsImV4cCI6MjA4MDU4NDY5NX0.1rJInVTDjf4f0sMNbyi6mkLJF185BDsH0u0Bld5j5xs
```

**Важно:** Для каждой переменной выберите **Environment**: `Production`, `Preview`, `Development` (или все три)

### Для Backend проекта (если деплоите отдельно)

Если backend деплоится как отдельный проект на Vercel:

1. Создайте новый проект в Vercel для backend
2. Перейдите в **Settings** → **Environment Variables**
3. Добавьте:

```env
# Supabase Configuration
SUPABASE_URL=https://njcsizoiirqfsrzvlzec.supabase.co
SUPABASE_SERVICE_ROLE_KEY=ваш-service-role-key-здесь

# Telegram Bot
BOT_TOKEN=ваш-telegram-bot-token

# JWT Secret
JWT_SECRET=ваш-jwt-secret-минимум-32-символа

# Admin IDs
ADMIN_TELEGRAM_IDS=123456789,987654321

# Environment
NODE_ENV=production
```

## 🔑 Шаг 2: Получение ключей Supabase

### Service Role Key (для Backend)

1. Откройте Supabase Dashboard: https://supabase.com/dashboard
2. Выберите проект `njcsizoiirqfsrzvlzec`
3. Перейдите в **Settings** → **API**
4. Найдите **service_role key** (секретный ключ)
5. Скопируйте его - это нужно для `SUPABASE_SERVICE_ROLE_KEY`

⚠️ **Важно:** Service Role Key имеет полный доступ к БД, не делитесь им публично!

## 🚀 Шаг 3: Деплой Frontend

### Вариант 1: Автоматический деплой (через Git)

1. Убедитесь, что все изменения закоммичены и запушены в GitHub
2. Vercel автоматически пересоберёт проект после push
3. Проверьте логи деплоя - не должно быть ошибок

### Вариант 2: Ручной деплой

1. В Vercel Dashboard → **Deployments**
2. Нажмите **Redeploy** на последнем деплое
3. Или используйте Vercel CLI:
   ```bash
   npm install -g vercel
   vercel --prod
   ```

## 🔧 Шаг 4: Деплой Backend (если отдельно)

### Настройка backend для Vercel

1. Убедитесь, что `backend/vercel.json` настроен правильно
2. В Vercel Dashboard создайте новый проект для backend
3. Подключите тот же GitHub репозиторий
4. Установите **Root Directory**: `backend`
5. Добавьте все переменные окружения из Шага 1
6. Деплой произойдёт автоматически

### Или используйте Vercel CLI:

```bash
cd backend
vercel --prod
```

## ✅ Шаг 5: Проверка работы

### Проверка Frontend

1. Откройте ваш сайт на Vercel
2. Проверьте консоль браузера (F12) - не должно быть ошибок
3. Попробуйте авторизоваться через Telegram
4. Данные должны загружаться из Supabase

### Проверка Backend

1. Проверьте логи в Vercel Dashboard
2. Должно быть сообщение: `📦 Используется Supabase`
3. Не должно быть ошибок подключения к БД
4. API endpoints должны отвечать

## 🔍 Шаг 6: Проверка подключения к Supabase

### В логах Vercel должно быть:

```
✅ Подключение к Supabase успешно
📦 Используется Supabase
```

### Если видите ошибки:

1. Проверьте, что все переменные окружения добавлены
2. Убедитесь, что ключи Supabase правильные
3. Проверьте, что миграции выполнены в Supabase
4. Проверьте логи в Supabase Dashboard

## 📝 Важные моменты

### 1. Root Directory

- **Frontend**: Установлен в `frontend` (через `vercel.json` или настройки Vercel)
- **Backend**: Если деплоите отдельно, установите `backend`

### 2. Переменные окружения

- Все переменные с префиксом `NEXT_PUBLIC_` доступны в браузере
- Остальные переменные доступны только на сервере
- Service Role Key должен быть только в backend переменных

### 3. База данных

- В production автоматически используется Supabase
- SQLite используется только локально
- Все данные должны быть мигрированы в Supabase

## 🐛 Решение проблем

### Проблема: "Module not found"

**Решение:**
- Убедитесь, что Root Directory установлен правильно
- Проверьте, что `vercel.json` в корне содержит правильные команды

### Проблема: "Cannot connect to Supabase"

**Решение:**
- Проверьте переменные окружения в Vercel
- Убедитесь, что ключи Supabase правильные
- Проверьте, что миграции выполнены

### Проблема: "404 Not Found"

**Решение:**
- Проверьте, что Root Directory установлен
- Убедитесь, что сборка прошла успешно
- Проверьте логи деплоя

## 🎉 Готово!

После выполнения всех шагов ваш проект должен работать на Vercel с подключением к Supabase!

---

**Если возникли проблемы, проверьте логи в Vercel Dashboard и Supabase Dashboard.**

