# Механизмы очистки и профилактики проблем

## Реализованные механизмы

### 1. Автоматическая очистка прошедших занятий при генерации
- При генерации новых занятий автоматически удаляются все прошедшие занятия (дата < сегодня)
- Удаляются также все связанные бронирования
- Работает в `backend/src/routes/recurringLessons.js` в endpoint `/api/recurring-lessons/generate`

### 2. Улучшенное удаление занятий
- При удалении занятия через админ-панель теперь всегда удаляются связанные бронирования
- Полное удаление из базы данных (не мягкое удаление)
- Работает в `backend/src/routes/lessons.js` в endpoint `DELETE /api/lessons/:id`

### 3. API endpoint для очистки прошедших занятий
- Новый endpoint: `DELETE /api/lessons/cleanup-past`
- Удаляет все занятия с датой < сегодня и связанные бронирования
- Доступен только администраторам
- Можно вызывать вручную через админ-панель

### 4. Кнопка в админ-панели
- Добавлена кнопка "Очистить прошедшие" в разделе расписания
- Позволяет вручную удалить все прошедшие занятия
- Находится рядом с кнопкой "Очистить расписание"

### 5. Скрипт для очистки
- `backend/scripts/cleanupPastLessons.js` - скрипт для ручной очистки
- Удаляет:
  - Прошедшие занятия (дата < сегодня)
  - Неактивные шаблоны и связанные занятия
  - Занятия с несуществующими шаблонами
- Можно запускать вручную: `node scripts/cleanupPastLessons.js`

### 6. Автоматическая очистка при старте сервера (опционально)
- Можно включить через переменную окружения `AUTO_CLEANUP_ON_START=true`
- При старте backend автоматически удаляет прошедшие занятия
- Настраивается в `.env` файле

### 7. Валидация шаблонов при генерации
- Генерация работает только с активными шаблонами
- Проверяется наличие всех обязательных полей:
  - `day_of_week` (1-7)
  - `start_time`, `end_time`
  - `hall_id`, `direction_id`, `trainer_id`
- Шаблоны с некорректными данными пропускаются

## Использование

### Ручная очистка через админ-панель
1. Откройте админ-панель → Расписание
2. Нажмите кнопку "🧹 Очистить прошедшие"
3. Подтвердите действие

### Ручная очистка через скрипт
```bash
cd backend
node scripts/cleanupPastLessons.js
```

### Автоматическая очистка при старте
Добавьте в `.env`:
```
AUTO_CLEANUP_ON_START=true
```

### API вызов
```javascript
// Через API
await lessonsAPI.cleanupPast()
```

## Что предотвращается

✅ Накопление прошедших занятий в базе данных  
✅ Занятия с некорректными данными из старых шаблонов  
✅ Занятия, ссылающиеся на несуществующие шаблоны  
✅ Накопление бронирований на удаленные занятия  
✅ Проблемы с отображением из-за устаревших данных  

## Рекомендации

1. **Регулярная очистка**: Рекомендуется периодически (раз в неделю/месяц) запускать очистку прошедших занятий
2. **Автоматическая очистка**: Для продакшена можно включить `AUTO_CLEANUP_ON_START=true`
3. **Перед генерацией**: Очистка прошедших занятий происходит автоматически при генерации новых занятий
4. **Мониторинг**: Проверяйте логи при генерации - там будет информация о количестве удаленных прошедших занятий




