# ✨ Интегрированы градиентные кнопки с анимацией

## 🎨 Что добавлено:

### Красивые анимированные кнопки с градиентом!

**Особенности:**
- 💜 **Фиолетовый градиент** (цвета студии #5833b6)
- ✨ **Плавная анимация** при наведении
- 🎭 **Динамические CSS свойства** (@property)
- 🌟 **Светящаяся рамка** с анимацией
- 📱 **Полностью адаптивные**

---

## 📦 Что было установлено:

### Новые зависимости:
- ✅ `@radix-ui/react-slot` - для композиции компонентов
- ✅ `class-variance-authority` - для вариантов стилей

### Новые файлы:
```
frontend/
├── components/
│   └── ui/
│       ├── beams-background.tsx  ← Анимированный фон
│       └── gradient-button.tsx   ← NEW! Градиентная кнопка
├── app/
│   ├── page.tsx                  ← Обновлена с новыми кнопками
│   └── globals.css               ← Добавлены стили градиентов
```

---

## 🎯 Как это работает:

### GradientButton компонент:
- Использует **CSS @property** для плавных анимаций
- **Radial-gradient** с 5 цветовыми остановками
- **Linear-gradient** для светящейся рамки
- **Transition** на 17+ CSS переменных!

### Эффекты:
1. **По умолчанию**: Фиолетовый градиент от темного к светлому
2. **При наведении**: Градиент меняет направление и цвета
3. **Рамка**: Светящаяся анимированная граница
4. **Transform**: Легкое увеличение при hover

---

## 🎨 Цветовая схема (фиолетовая):

### Default состояние:
```css
--color-1: #1a0b2e  /* Темно-фиолетовый */
--color-2: #4527a0  /* Фиолетовый темный */
--color-3: #5833b6  /* Основной цвет студии */
--color-4: #7e57c2  /* Фиолетовый светлый */
--color-5: #9575cd  /* Светло-фиолетовый */
```

### Hover состояние:
```css
/* Градиент переворачивается в обратную сторону */
--color-1: #9575cd  /* Светлый становится темным */
--color-2: #7e57c2
--color-3: #5833b6
--color-4: #4527a0
--color-5: #1a0b2e  /* Темный становится светлым */
```

---

## 📱 Где используется:

### Главная страница:
- 📅 Кнопка "Записаться"
- 💃 Кнопка "О направлениях"
- 💰 Кнопка "Цены"
- 👤 Кнопка "Личный кабинет"

Все 4 кнопки теперь с **градиентной анимацией**!

---

## 🎛️ Варианты использования:

### Default вариант (фиолетовый):
```tsx
<GradientButton onClick={handleClick}>
  Нажми меня
</GradientButton>
```

### Variant (голубой/зеленый):
```tsx
<GradientButton variant="variant" onClick={handleClick}>
  Другой стиль
</GradientButton>
```

### С иконкой:
```tsx
<GradientButton>
  <div className="flex items-center gap-2">
    <span>📅</span>
    <span>Записаться</span>
  </div>
</GradientButton>
```

---

## 🔧 Кастомизация:

### Изменить цвета градиента:

В `globals.css` найдите:

```css
@property --color-1 {
  initial-value: #1a0b2e;  /* Измените здесь */
}
@property --color-2 {
  initial-value: #4527a0;  /* И здесь */
}
/* и так далее... */
```

### Изменить скорость анимации:

```css
.gradient-button {
  transition:
    --pos-x 0.5s,  /* ← Измените на 0.3s для быстрее */
    --pos-y 0.5s,
    /* ... */
}
```

### Создать свой вариант:

В `globals.css` добавьте:

```css
.gradient-button-my-variant {
  --color-1: #ff0000;
  --color-2: #ff6600;
  --color-3: #ffcc00;
  /* ... */
}
```

Использование:
```tsx
<GradientButton variant="my-variant">
  Моя кнопка
</GradientButton>
```

---

## 🎬 Технические детали:

### CSS @property:
Позволяет анимировать CSS переменные!
```css
@property --pos-x {
  syntax: '<percentage>';    /* Тип значения */
  initial-value: 11.14%;     /* Начальное значение */
  inherits: false;           /* Не наследуется */
}
```

### Radial-gradient:
```css
background: radial-gradient(
  var(--spread-x) var(--spread-y) at var(--pos-x) var(--pos-y),
  var(--color-1) var(--stop-1),  /* 5 цветовых остановок */
  var(--color-2) var(--stop-2),
  var(--color-3) var(--stop-3),
  var(--color-4) var(--stop-4),
  var(--color-5) var(--stop-5)
);
```

### Светящаяся рамка (::before):
```css
.gradient-button::before {
  background: linear-gradient(
    var(--border-angle),          /* Угол градиента */
    var(--border-color-1),        /* Цвет 1 */
    var(--border-color-2)         /* Цвет 2 */
  );
  /* Магия с mask для создания только границы */
}
```

---

## ⚡ Производительность:

- ✅ Использует CSS для анимации (GPU-ускорение)
- ✅ Нет JavaScript анимаций
- ✅ Transition вместо animation (более плавно)
- ✅ Will-change автоматически применяется браузером

---

## 🎨 Совместимость:

✅ Chrome 85+
✅ Edge 85+
✅ Safari 15.4+
✅ Firefox 106+ (с флагом)

**Примечание**: @property поддерживается не во всех браузерах, но кнопка останется рабочей (просто без плавной анимации градиента)

---

## 💡 Примеры использования на других страницах:

### В форме:
```tsx
<form onSubmit={handleSubmit}>
  <input type="email" />
  <GradientButton type="submit">
    Отправить
  </GradientButton>
</form>
```

### В списке:
```tsx
{items.map(item => (
  <GradientButton key={item.id} onClick={() => select(item)}>
    {item.name}
  </GradientButton>
))}
```

### С загрузкой:
```tsx
<GradientButton disabled={loading}>
  {loading ? 'Загрузка...' : 'Сохранить'}
</GradientButton>
```

---

## 🎉 Результат:

Теперь у приложения "Геометрия":
- 💜 Красивые градиентные кнопки
- ✨ Плавная анимация при hover
- 🎭 Профессиональный внешний вид
- 🌟 Уникальный дизайн

---

**Обновите страницу и наведите курсор на кнопки - увидите магию! ✨💜**

