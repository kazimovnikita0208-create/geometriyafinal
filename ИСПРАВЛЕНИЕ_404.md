# 🔧 Исправление ошибки 404 NOT_FOUND

## ❌ Проблема

После деплоя на Vercel появляется ошибка **404: NOT_FOUND**.

## ✅ Решение

### Шаг 1: Проверьте Root Directory в Vercel

1. Откройте Vercel Dashboard: https://vercel.com/dashboard
2. Выберите проект `geometriyafinal`
3. Перейдите в **Settings** → **Build and Deployment**
4. Проверьте **Root Directory**:
   - Должно быть: `frontend`
   - Если пусто или неправильно - установите `frontend` и нажмите **Save**

### Шаг 2: Проверьте Output Directory

В том же разделе проверьте:
- **Output Directory** должен быть: `.next` (не `frontend/.next`!)
- Если Root Directory = `frontend`, то Output Directory = `.next`

### Шаг 3: Проверьте Build Command

- **Build Command**: `npm run build`
- **Install Command**: `npm install`

### Шаг 4: Пересоберите проект

1. Перейдите в **Deployments**
2. Нажмите **Redeploy** на последнем деплое
3. Или сделайте новый commit

## 🔍 Что проверить

### В Vercel Dashboard → Settings → Build and Deployment:

- ✅ **Root Directory**: `frontend`
- ✅ **Build Command**: `npm run build`
- ✅ **Output Directory**: `.next`
- ✅ **Install Command**: `npm install`
- ✅ **Node.js Version**: 24.x (или 22.x)

## ⚠️ Важно

Если Root Directory = `frontend`, то:
- Output Directory должен быть `.next` (относительно `frontend/`)
- НЕ `frontend/.next`!

---

**Проверьте настройки в Vercel Dashboard и пересоберите проект!**

