# 🤖 Запуск простого бота

Быстрая инструкция для запуска Telegram-бота с кнопкой открытия Mini App.

## Шаг 1: Установите Node.js

Если ещё не установлен, скачайте с https://nodejs.org (версия 18 или новее)

## Шаг 2: Установите зависимости

Откройте терминал (PowerShell или CMD) в папке проекта и выполните:

```bash
npm install
```

Это установит библиотеку `node-telegram-bot-api` для работы с Telegram.

## Шаг 3: Обновите URL приложения

⚠️ **ВАЖНО!** Откройте файл `simple-bot.js` и найдите строку:

```javascript
const MINI_APP_URL = 'https://your-app-url.com';
```

Замените её на:

### Вариант А: Для теста (пока нет приложения)
```javascript
const MINI_APP_URL = 'https://google.com'; // Временный URL
```

### Вариант Б: Когда запустите frontend с ngrok
```javascript
const MINI_APP_URL = 'https://abc123.ngrok.io'; // Ваш ngrok URL
```

### Вариант В: После деплоя на Vercel
```javascript
const MINI_APP_URL = 'https://geometriya-mini-app.vercel.app';
```

## Шаг 4: Запустите бота

```bash
npm start
```

Вы увидите:
```
🤖 Бот запущен!
✅ Бот готов к работе!
💡 Отправьте /start боту в Telegram для проверки
```

## Шаг 5: Протестируйте в Telegram

1. Откройте Telegram
2. Найдите вашего бота (по username, который вы создали в BotFather)
3. Отправьте команду `/start`
4. Вы должны увидеть приветственное сообщение с кнопками!

## 🎯 Доступные команды

- `/start` - Главное меню с кнопкой приложения
- `/help` - Помощь

## 🔧 Настройка Mini App URL в BotFather

После запуска frontend (локально с ngrok или на Vercel), настройте URL в BotFather:

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/mybots`
3. Выберите вашего бота
4. Нажмите "Bot Settings" → "Menu Button"
5. Выберите "Edit Menu Button URL"
6. Введите URL вашего Mini App (ngrok или Vercel)
7. Введите текст кнопки: "Открыть приложение"

Теперь кнопка будет работать правильно!

## 🐛 Решение проблем

### Ошибка "node-telegram-bot-api not found"
```bash
npm install
```

### Ошибка "EFATAL: Error: getaddrinfo ENOTFOUND api.telegram.org"
Проверьте интернет-соединение или VPN

### Бот не отвечает
1. Убедитесь, что бот запущен (в терминале должно быть "Бот запущен")
2. Проверьте токен бота (он уже прописан в `simple-bot.js`)
3. Попробуйте перезапустить: Ctrl+C, затем `npm start`

### Кнопка "Открыть приложение" не работает
1. Убедитесь, что MINI_APP_URL использует HTTPS (не HTTP!)
2. Ngrok автоматически даёт HTTPS URL
3. Vercel тоже использует HTTPS

## 🚀 Следующие шаги

1. ✅ Бот работает
2. ⏳ Запустите frontend (см. QUICKSTART.md)
3. ⏳ Настройте ngrok для локального тестирования
4. ⏳ Обновите MINI_APP_URL в `simple-bot.js`
5. ⏳ Настройте Menu Button в BotFather

---

**Токен бота уже прописан:** `8486427372:AAEDwcPL0pt3WCAc8JT1JwY-TKZqn3ZsbLM`

⚠️ **Безопасность:** Не публикуйте этот токен в открытых репозиториях!

---

## 💡 Примечание

Этот файл (`simple-bot.js`) - упрощённая версия для быстрого тестирования.

Для полноценной разработки используйте структуру из папки `backend/` согласно DEVELOPMENT_PLAN.md.

