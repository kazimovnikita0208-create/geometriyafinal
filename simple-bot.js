/**
 * Простой Telegram-бот для студии "Геометрия"
 * С кнопкой для открытия Mini App
 */

const TelegramBot = require('node-telegram-bot-api');

// Токен бота (получен от BotFather)
const BOT_TOKEN = '8486427372:AAEDwcPL0pt3WCAc8JT1JwY-TKZqn3ZsbLM';

// URL Mini App - задеплоенное приложение на Vercel
const MINI_APP_URL = 'https://frontend-phi-mocha-55.vercel.app';

// Создаём бота
const bot = new TelegramBot(BOT_TOKEN, { polling: true });

console.log('🤖 Бот запущен!');

// Команда /start
bot.onText(/\/start/, (msg) => {
  const chatId = msg.chat.id;
  const firstName = msg.from.first_name || 'друг';
  
  const welcomeMessage = `
👋 Привет, ${firstName}!

Добро пожаловать в студию танцев и фитнеса на пилоне *«Геометрия»*! 💜

🏢 Наши залы:
📍 ул. Волгина, 117А
📍 Московское шоссе, 43, ТОЦ "Охотный ряд"

💃 Направления:
• Pole Fit
• Pole Exotic
• Сила & Гибкость
• Растяжка
• High Heels (Choreo)

Нажмите кнопку ниже, чтобы открыть приложение и записаться на занятие! ⬇️
  `.trim();
  
  // Отправляем сообщение с кнопкой для открытия Mini App
  bot.sendMessage(chatId, welcomeMessage, {
    parse_mode: 'Markdown',
    reply_markup: {
      inline_keyboard: [
        [
          {
            text: '📱 Открыть приложение',
            web_app: { url: MINI_APP_URL }
          }
        ],
        [
          { text: '📞 Контакты', callback_data: 'contacts' },
          { text: 'ℹ️ Помощь', callback_data: 'help' }
        ]
      ]
    }
  });
});

// Команда /help
bot.onText(/\/help/, (msg) => {
  const chatId = msg.chat.id;
  
  const helpMessage = `
📖 *Помощь*

🏢 Студия танцев и фитнеса на пилоне

🔹 Используйте кнопку "Открыть приложение" для:
  • Записи на занятия
  • Просмотра расписания
  • Управления абонементом
  • Выбора тарифа

💃 *Наши направления:*
• Pole Fit
• Pole Exotic
• Сила & Гибкость
• Растяжка
• High Heels (Choreo)

📞 *Контакты:*
• Телефон: +7 (XXX) XXX-XX-XX
• ВКонтакте: geometria163
  `.trim();
  
  bot.sendMessage(chatId, helpMessage, {
    parse_mode: 'Markdown',
    reply_markup: {
      inline_keyboard: [
        [
          {
            text: '📱 Открыть приложение',
            web_app: { url: MINI_APP_URL }
          }
        ]
      ]
    }
  });
});

// Обработка нажатий на инлайн-кнопки
bot.on('callback_query', (query) => {
  const chatId = query.message.chat.id;
  const data = query.data;
  
  // Отвечаем на callback query (убирает "часики" на кнопке)
  bot.answerCallbackQuery(query.id);
  
  switch (data) {
    case 'contacts':
      bot.sendMessage(chatId, `
📞 *Контакты студии "Геометрия"*

🏢 Студия танцев и фитнеса на пилоне

📍 *Залы:*
• ул. Волгина, 117А
• Московское шоссе, 43, ТОЦ "Охотный ряд"

📱 Телефон: +7 (XXX) XXX-XX-XX
💜 ВКонтакте: geometria163

⏰ Время работы: ежедневно 10:00 - 22:00

Будем рады видеть вас на занятиях! 💜
      `.trim(), {
        parse_mode: 'Markdown'
      });
      break;
      
    case 'help':
      bot.sendMessage(chatId, `
ℹ️ *Информация*

*Наши направления:*
💪 Pole Fit - трюковая дисциплина на пилоне
💃 Pole Exotic - танец на каблуках с пилоном
🏋️ Сила & Гибкость - ускорьте свой прогресс
🤸 Растяжка - грациозные прогибы и шпагаты
👠 High Heels - танец на каблуках

*Как записаться на занятие?*
1. Нажмите "Открыть приложение"
2. Выберите "Записаться"
3. Выберите направление, тренера и время
4. Подтвердите запись

*Как отменить занятие?*
1. Откройте "Личный кабинет"
2. Найдите нужное занятие
3. Нажмите "Отменить"

*Вопросы?*
Звоните: +7 (XXX) XXX-XX-XX
      `.trim(), {
        parse_mode: 'Markdown',
        reply_markup: {
          inline_keyboard: [
            [
              {
                text: '📱 Открыть приложение',
                web_app: { url: MINI_APP_URL }
              }
            ]
          ]
        }
      });
      break;
  }
});

// Обработка ошибок
bot.on('polling_error', (error) => {
  console.error('❌ Ошибка polling:', error.code, error.message);
});

bot.on('error', (error) => {
  console.error('❌ Ошибка бота:', error.message);
});

// Обработка остановки
process.on('SIGINT', () => {
  console.log('\n👋 Бот остановлен');
  bot.stopPolling();
  process.exit(0);
});

console.log('✅ Бот готов к работе!');
console.log('💡 Отправьте /start боту в Telegram для проверки');
console.log('\n⚠️  ВАЖНО: Обновите MINI_APP_URL на реальный адрес вашего приложения!');

