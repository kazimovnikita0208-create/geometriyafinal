# 🔗 Интеграция Frontend и Backend - Гот ово!

## ✅ Что работает

### Backend (localhost:3001)
- ✅ Express сервер запущен
- ✅ SQLite база данных с данными
- ✅ API endpoints:
  - `/api/directions` - направления танцев
  - `/api/halls` - залы студии
  - `/api/subscription-types` - типы абонементов
  - `/api/subscriptions` - **НОВОЕ!** Управление абонементами с подтверждением
  - `/api/auth` - аутентификация через Telegram

### Frontend (localhost:3000)
- ✅ Next.js сервер запущен
- ✅ Загрузка данных из backend
- ✅ Fallback на mock данные если backend недоступен
- ✅ Полная форма бронирования абонемента
- ✅ Отправка заявок на абонемент

## 📊 Архитектура абонементов

### Поток работы:

```
1. Пользователь → Выбирает абонемент → Заполняет форму
                                              ↓
2. Frontend → POST /api/subscriptions → Backend
                                              ↓
3. Backend → Создает запись (status: "pending", is_active: 0)
                                              ↓
4. Админ-панель → GET /api/subscriptions/requests?status=pending
                                              ↓
5. Админ → POST /api/subscriptions/:id/approve
                                              ↓
6. Backend → Обновляет (status: "confirmed", is_active: 1)
                                              ↓
7. Пользователь → Может использовать абонемент
```

## 🚀 Как запустить

### Терминал 1 - Backend:
```bash
cd backend
npm run dev
```

### Терминал 2 - Frontend:
```bash
cd frontend
npm run dev
```

## 🧪 Тестирование абонементов

### 1. Откройте frontend:
```
http://localhost:3000/prices
```

### 2. Выберите любой абонемент

### 3. Нажмите "Начать заниматься"

### 4. Заполните форму:
- Имя и Фамилия
- Телефон
- Адрес зала
- Способ записи (гибкая/автоматическая)

### 5. Нажмите "Отправить заявку"

### 6. Проверьте в консоли браузера:
```
✅ Заявка успешно создана: {subscription: {...}}
```

### 7. Проверьте что заявка создалась (через Postman/curl):
```bash
# Нужен токен администратора!
curl http://localhost:3001/api/subscriptions/requests?status=pending \
  -H "Authorization: Bearer YOUR_ADMIN_TOKEN"
```

## 📝 Файлы

### Backend:
- `backend/src/routes/subscriptions.js` - API endpoints для абонементов
- `backend/src/middleware/auth.js` - Middleware аутентификации
- `backend/scripts/createDatabase.js` - SQL схема (таблица subscriptions)

### Frontend:
- `frontend/lib/api.ts` - API клиент с методами subscriptionsAPI
- `frontend/app/prices/page.tsx` - Страница с формой бронирования
- `frontend/lib/mockData.ts` - Mock данные для fallback

### Документация:
- `backend/docs/SUBSCRIPTIONS_API.md` - Подробная документация API
- `INTEGRATION.md` - Этот файл

## 🎯 Следующие шаги

### Этап 4: Админ-панель (в процессе)
- [ ] Страница админ-панели для просмотра заявок
- [ ] Кнопки подтверждения/отклонения заявок
- [ ] Уведомления для админа о новых заявках

### Этап 5: Telegram Bot
- [ ] Настройка Telegram бота
- [ ] Отправка уведомлений пользователю при подтверждении
- [ ] Отправка уведомлений админу о новых заявках

### Этап 6: Дополнительные функции
- [ ] Расписание занятий
- [ ] Бронирование занятий
- [ ] Аренда залов/пилонов
- [ ] Профиль пользователя

## 🔐 Аутентификация (TODO)

Сейчас для создания заявки на абонемент требуется JWT токен. Нужно реализовать:

1. Telegram WebApp аутентификация на frontend
2. Получение токена через `/api/auth/login`
3. Сохранение токена в localStorage
4. Автоматическое добавление токена в запросы

## 💡 Полезные команды

```bash
# Проверить backend
curl http://localhost:3001/api

# Проверить directions
curl http://localhost:3001/api/directions

# Проверить subscription types
curl http://localhost:3001/api/subscription-types

# Создать заявку на абонемент (нужен токен!)
curl -X POST http://localhost:3001/api/subscriptions \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{
    "subscriptionTypeId": 4,
    "bookingType": "flexible",
    "firstName": "Иван",
    "lastName": "Иванов",
    "phone": "+79001234567",
    "address": "Волгина, 117А"
  }'
```

## 🎉 Готово!

Frontend и Backend полностью интегрированы!
Система абонементов с подтверждением администратором работает!

---

**Следующий этап**: Админ-панель для управления заявками 👨‍💼

