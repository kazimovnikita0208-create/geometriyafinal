# Тестирование системы абонементов

## ✅ Что исправлено:

### 1. **Backend**
- ✅ CORS настроен на порт 3002 (frontend)
- ✅ API работает на `http://localhost:3001`
- ✅ Поле `address` теперь обязательное в БД
- ✅ Добавлено поле `rejection_reason` для причины отказа

### 2. **Frontend**  
- ✅ Создан модуль авторизации `lib/auth.ts`
- ✅ Автоматическая авторизация через Telegram при загрузке страницы
- ✅ Проверка авторизации перед отправкой заявки
- ✅ Личный кабинет загружает абонементы с backend
- ✅ Компонент `SubscriptionCard` для красивого отображения

## 🧪 Как протестировать:

### Шаг 1: Откройте приложение
```
http://localhost:3002
```

### Шаг 2: Перейдите на страницу "Абонементы"
```
http://localhost:3002/prices
```

### Шаг 3: Выберите любой абонемент
- Нажмите "Начать заниматься"
- Заполните форму:
  - Имя: **Никита**
  - Фамилия: **Тестов**
  - Телефон: **89397187500**
  - Адрес зала: **ТОЦ "Охотный ряд"**
  - Способ записи: **Гибкая запись** или **Автоматическая**
  
### Шаг 4: Отправьте заявку
- Нажмите "Отправить заявку"
- Должно появиться сообщение: **"✅ Заявка отправлена!"**
- Через 1.5 секунды вас перенаправит в **Личный кабинет**

### Шаг 5: Проверьте личный кабинет
```
http://localhost:3002/profile
```

Вы должны увидеть:
- 🟡 Карточку абонемента со статусом **"Ожидает подтверждения"**
- Желтый цвет и значок часов
- Сообщение: "Ваша заявка на рассмотрении..."

## 🔧 Возможные проблемы:

### ❌ "Failed to fetch"
**Причина**: Backend не запущен или CORS  
**Решение**: 
```bash
cd backend
node src/index.js
```

### ❌ "Ошибка авторизации"
**Причина**: Приложение открыто НЕ через Telegram  
**Решение**: 
- Для тестирования без Telegram:
  1. Откройте DevTools (F12)
  2. Console → выполните:
  ```javascript
  localStorage.setItem('token', 'test-token')
  ```
  3. Обновите страницу

**Временное решение для разработки**: Отключить проверку авторизации в `backend/src/middleware/auth.js`

## 📊 Проверка в базе данных:

### Посмотреть все заявки:
```javascript
// В консоли браузера (F12)
fetch('http://localhost:3001/api/subscriptions/my', {
  headers: {
    'Authorization': 'Bearer ' + localStorage.getItem('token')
  }
})
.then(r => r.json())
.then(console.log)
```

### Посмотреть БД напрямую:
```bash
cd backend
sqlite3 dev.db
SELECT * FROM subscriptions;
.exit
```

## 🎯 Следующий шаг: Админ-панель

После успешного тестирования создания заявок, нужно добавить в админ-панель:
1. Просмотр всех заявок (`GET /api/subscriptions/requests`)
2. Кнопки "Подтвердить" и "Отклонить"
3. Поле для ввода причины отказа

Это позволит администратору управлять заявками на абонементы.

