# 📋 Шаги миграции данных в Supabase

## ✅ Шаг 1: Таблицы созданы
Вы уже выполнили SQL скрипт - все таблицы созданы в Supabase!

## 🔐 Шаг 2: Получить Service Role Key

1. Откройте Supabase Dashboard: https://supabase.com/dashboard
2. Выберите ваш проект
3. Перейдите в **Settings** → **API**
4. Найдите раздел **Project API keys**
5. Скопируйте **service_role key** (секретный ключ, не anon key!)

⚠️ **Важно:** Service Role Key имеет полный доступ к БД. Никогда не коммитьте его в Git!

## ⚙️ Шаг 3: Настроить переменные окружения

Создайте файл `backend/.env` (если его еще нет) и добавьте:

```env
SUPABASE_URL=https://njcsizoiirqfsrzvlzec.supabase.co
SUPABASE_SERVICE_ROLE_KEY=ваш-service-role-key-здесь
```

**Где взять Service Role Key:**
- Supabase Dashboard → Settings → API → service_role key (секретный)

## 🚀 Шаг 4: Запустить миграцию данных

```bash
cd backend
node scripts/migrateToSupabase.js
```

Скрипт автоматически:
- ✅ Подключится к SQLite (файл `dev.db`)
- ✅ Подключится к Supabase
- ✅ Перенесет все данные из всех таблиц
- ✅ Покажет прогресс и результаты

## ✅ Шаг 5: Проверить результат

После завершения миграции:

1. Откройте Supabase Dashboard → **Table Editor**
2. Проверьте каждую таблицу:
   - `users` - должны быть пользователи
   - `halls` - должны быть залы
   - `directions` - должны быть направления
   - `trainers` - должны быть тренеры
   - `subscription_types` - должны быть типы абонементов
   - `subscriptions` - должны быть абонементы
   - `lessons` - должны быть занятия
   - `bookings` - должны быть записи
   - `rental_bookings` - должны быть аренды
   - `notifications` - должны быть уведомления
   - `settings` - должны быть настройки

3. Сравните количество записей с SQLite

## 🐛 Если возникли ошибки

### Ошибка: "Cannot find module '@supabase/supabase-js'"
**Решение:** 
```bash
cd backend
npm install @supabase/supabase-js
```

### Ошибка: "Missing SUPABASE_URL or SUPABASE_KEY"
**Решение:** Проверьте, что файл `backend/.env` существует и содержит правильные переменные

### Ошибка: "permission denied"
**Решение:** Убедитесь, что используете **Service Role Key**, а не Anon Key

### Ошибка: "relation does not exist"
**Решение:** Убедитесь, что вы выполнили SQL скрипт создания таблиц

---

**Готово к миграции! Следуйте шагам выше! 🚀**

