# 🔧 Полная настройка Supabase для работы на Vercel

## 📋 Текущее состояние

✅ **Уже настроено:**
- Frontend клиенты Supabase (client, server, middleware)
- Backend адаптер для работы с Supabase
- Миграции базы данных выполнены
- Данные мигрированы в Supabase

## 🚀 Шаг 1: Проверка миграций в Supabase

### 1.1 Откройте Supabase Dashboard

1. Перейдите на https://supabase.com/dashboard
2. Выберите проект `njcsizoiirqfsrzvlzec`
3. Перейдите в **SQL Editor**

### 1.2 Проверьте, что все таблицы созданы

Выполните запрос для проверки:

```sql
-- Проверка всех таблиц
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public' 
ORDER BY table_name;
```

**Должны быть таблицы:**
- users
- halls
- directions
- trainers
- lessons
- bookings
- subscriptions
- subscription_types
- notifications
- rental_bookings
- prices

### 1.3 Если таблиц нет - выполните миграции

Если какие-то таблицы отсутствуют, выполните миграции в порядке:

1. `supabase/migrations/001_initial_schema.sql`
2. `supabase/migrations/002_fix_identity_columns.sql`
3. `supabase/migrations/003_fix_lessons_day_of_week.sql`
4. `supabase/migrations/004_remove_duplicate_halls.sql`

## 🔑 Шаг 2: Получение ключей Supabase

### 2.1 Откройте настройки API

1. В Supabase Dashboard перейдите в **Settings** → **API**
2. Найдите следующие ключи:

#### Anon/Public Key (для Frontend)
- **Ключ:** `anon` / `public`
- **Использование:** Frontend, клиентские запросы
- **Текущее значение:** `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im5qY3Npem9paXJxZnNyenZsemVjIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjUwMDg2OTUsImV4cCI6MjA4MDU4NDY5NX0.1rJInVTDjf4f0sMNbyi6mkLJF185BDsH0u0Bld5j5xs`

#### Service Role Key (для Backend)
- **Ключ:** `service_role` (секретный!)
- **Использование:** Backend, серверные запросы
- **⚠️ ВАЖНО:** Этот ключ имеет полный доступ к БД, не публикуйте его!

### 2.2 Project URL

- **URL:** `https://njcsizoiirqfsrzvlzec.supabase.co`

## ⚙️ Шаг 3: Настройка переменных окружения в Vercel

### 3.1 Для Frontend проекта

1. Откройте Vercel Dashboard: https://vercel.com/dashboard
2. Выберите проект `geometriyafinal`
3. Перейдите в **Settings** → **Environment Variables**
4. Добавьте/проверьте следующие переменные:

#### Обязательные переменные:

| Ключ | Значение | Environment |
|------|----------|-------------|
| `NEXT_PUBLIC_SUPABASE_URL` | `https://njcsizoiirqfsrzvlzec.supabase.co` | Production, Preview, Development |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...` | Production, Preview, Development |
| `NEXT_PUBLIC_API_URL` | `https://ваш-backend.vercel.app` | Production, Preview, Development |

**Как добавить:**
1. Нажмите **Add New**
2. Введите **Key** (например, `NEXT_PUBLIC_SUPABASE_URL`)
3. Введите **Value** (например, `https://njcsizoiirqfsrzvlzec.supabase.co`)
4. Выберите **Environment** (Production, Preview, Development - или все три)
5. Нажмите **Save**

### 3.2 Для Backend проекта (если деплоите отдельно)

Если backend деплоится как отдельный проект:

1. Создайте новый проект в Vercel для backend
2. Перейдите в **Settings** → **Environment Variables**
3. Добавьте:

| Ключ | Значение | Где взять |
|------|----------|-----------|
| `SUPABASE_URL` | `https://njcsizoiirqfsrzvlzec.supabase.co` | Supabase Dashboard → Settings → API |
| `SUPABASE_SERVICE_ROLE_KEY` | `ваш-service-role-key` | Supabase Dashboard → Settings → API → service_role |
| `BOT_TOKEN` | `ваш-telegram-bot-token` | [@BotFather](https://t.me/BotFather) |
| `JWT_SECRET` | `секретный-ключ-минимум-32-символа` | Сгенерировать или из `backend/.env` |
| `ADMIN_TELEGRAM_IDS` | `123456789,987654321` | Ваши Telegram ID через запятую |
| `NODE_ENV` | `production` | - |

## 🔍 Шаг 4: Проверка конфигурации

### 4.1 Проверка Frontend клиентов

Убедитесь, что файлы существуют и настроены правильно:

- ✅ `frontend/utils/supabase/client.ts` - для клиентских компонентов
- ✅ `frontend/utils/supabase/server.ts` - для серверных компонентов
- ✅ `frontend/utils/supabase/middleware.ts` - для middleware
- ✅ `frontend/middleware.ts` - Next.js middleware

### 4.2 Проверка Backend адаптера

Убедитесь, что:
- ✅ `backend/src/config/supabase.js` - конфигурация Supabase
- ✅ `backend/src/config/database-adapter.js` - адаптер БД
- ✅ Backend автоматически переключается на Supabase в production

**Логика переключения:**
```javascript
// В database-adapter.js
const USE_SUPABASE = isSupabaseEnabled() || process.env.NODE_ENV === 'production';
```

Это означает, что в production (на Vercel) автоматически используется Supabase, если настроены переменные окружения.

## 🚀 Шаг 5: Деплой и проверка

### 5.1 Деплой Frontend

1. Убедитесь, что все переменные окружения добавлены в Vercel
2. Перейдите в **Deployments**
3. Нажмите **Redeploy** на последнем деплое
4. Или сделайте новый commit - Vercel автоматически пересоберёт

### 5.2 Проверка логов

После деплоя проверьте логи:

1. Перейдите в **Deployments** → выберите последний деплой
2. Откройте **Build Logs**
3. Проверьте, что нет ошибок

### 5.3 Проверка работы приложения

1. Откройте ваш сайт на Vercel
2. Откройте консоль браузера (F12)
3. Проверьте, что нет ошибок подключения к Supabase
4. Попробуйте авторизоваться через Telegram
5. Проверьте, что данные загружаются

### 5.4 Проверка Backend (если отдельно)

Если backend деплоится отдельно:

1. Проверьте логи в Vercel
2. Должно быть сообщение: `📦 Используется Supabase`
3. Должно быть сообщение: `✅ Подключение к Supabase успешно`
4. API endpoints должны отвечать

## 🐛 Решение проблем

### Проблема 1: "Cannot connect to Supabase"

**Решение:**
1. Проверьте переменные окружения в Vercel
2. Убедитесь, что ключи правильные
3. Проверьте, что Project URL правильный
4. Проверьте, что проект Supabase не приостановлен

### Проблема 2: "Table does not exist"

**Решение:**
1. Выполните миграции в Supabase SQL Editor
2. Проверьте, что все таблицы созданы
3. Проверьте названия таблиц (должны быть в нижнем регистре)

### Проблема 3: "Backend использует SQLite вместо Supabase"

**Решение:**
1. Проверьте переменные окружения в Vercel для backend
2. Убедитесь, что `SUPABASE_URL` и `SUPABASE_SERVICE_ROLE_KEY` добавлены
3. Проверьте, что `NODE_ENV=production`
4. Пересоберите проект

### Проблема 4: "Данные не загружаются"

**Решение:**
1. Проверьте, что данные мигрированы в Supabase
2. Проверьте логи в Supabase Dashboard → Logs
3. Проверьте Row Level Security (RLS) политики
4. Убедитесь, что таблицы не пустые

## ✅ Чеклист перед деплоем

- [ ] Все миграции выполнены в Supabase
- [ ] Данные мигрированы в Supabase
- [ ] Переменные окружения добавлены в Vercel для Frontend
- [ ] Переменные окружения добавлены в Vercel для Backend (если отдельно)
- [ ] Service Role Key получен из Supabase
- [ ] Anon Key добавлен в переменные окружения
- [ ] Project URL правильный
- [ ] Backend адаптер настроен правильно
- [ ] Frontend клиенты настроены правильно

## 🎉 После настройки

После выполнения всех шагов:

1. ✅ Frontend будет работать с Supabase
2. ✅ Backend будет работать с Supabase в production
3. ✅ Все данные будут браться из Supabase
4. ✅ Приложение будет полностью функциональным на Vercel

---

**Если возникли проблемы, проверьте логи в Vercel и Supabase Dashboard!**

