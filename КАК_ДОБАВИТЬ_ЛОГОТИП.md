# 🎨 Как добавить свой логотип на главную страницу

## 📋 Краткая инструкция:

### Способ 1: PNG/SVG файл (рекомендуется)

1. **Подготовьте логотип**:
   - Формат: PNG (с прозрачностью) или SVG
   - Размер: 200x200px или больше
   - Цвет: желательно белый или светлый (т.к. фон темный)

2. **Положите файл в папку**:
   ```
   frontend/public/logo.png
   ```
   или
   ```
   frontend/public/logo.svg
   ```

3. **Замените код в `frontend/app/page.tsx`**:
   
   Найдите блок (строки ~90-102):
   ```tsx
   {/* Премиум иконка вместо эмодзи */}
   <div className="mx-auto w-16 h-16 sm:w-20 sm:h-20 relative">
     ...весь этот блок...
   </div>
   ```

   Замените на:
   ```tsx
   {/* Ваш логотип */}
   <div className="mx-auto mb-6">
     <img 
       src="/logo.png" 
       alt="Геометрия" 
       className="w-24 h-24 sm:w-32 sm:h-32 object-contain"
     />
   </div>
   ```

4. **Сохраните и обновите страницу** (F5)

---

## 📝 Детальные варианты:

### Вариант A: Простое изображение (PNG/SVG)

#### Шаг 1: Добавьте файл
Положите ваш логотип в:
```
frontend/
└── public/
    └── logo.png  ← Ваш файл
```

#### Шаг 2: Код для страницы
```tsx
{/* Логотип */}
<div className="mx-auto mb-6">
  <img 
    src="/logo.png" 
    alt="Геометрия" 
    className="w-24 h-24 sm:w-32 sm:h-32 object-contain"
  />
</div>
```

**Размеры:**
- Мобильные: 96x96px (`w-24 h-24`)
- Десктоп: 128x128px (`w-32 h-32`)

---

### Вариант B: С эффектом свечения

```tsx
{/* Логотип с эффектом свечения */}
<div className="mx-auto mb-6 relative">
  {/* Свечение */}
  <div className="absolute inset-0 bg-purple-500/20 blur-3xl rounded-full"></div>
  
  {/* Сам логотип */}
  <img 
    src="/logo.png" 
    alt="Геометрия" 
    className="relative w-24 h-24 sm:w-32 sm:h-32 object-contain filter drop-shadow-2xl"
  />
</div>
```

---

### Вариант C: В рамке с градиентом

```tsx
{/* Логотип в рамке */}
<div className="mx-auto mb-6">
  <div className="relative w-28 h-28 sm:w-36 sm:h-36">
    {/* Градиентная рамка */}
    <div className="absolute inset-0 bg-gradient-to-br from-purple-400 to-purple-600 rounded-2xl p-1">
      <div className="w-full h-full bg-black/80 rounded-2xl flex items-center justify-center">
        <img 
          src="/logo.png" 
          alt="Геометрия" 
          className="w-20 h-20 sm:w-28 sm:h-28 object-contain"
        />
      </div>
    </div>
  </div>
</div>
```

---

### Вариант D: SVG логотип (встроенный)

Если у вас SVG, можете вставить код прямо в компонент:

```tsx
{/* SVG логотип */}
<div className="mx-auto mb-6">
  <svg 
    className="w-24 h-24 sm:w-32 sm:h-32 text-purple-300"
    viewBox="0 0 100 100"
    fill="currentColor"
  >
    {/* Вставьте сюда path из вашего SVG */}
    <path d="M50,10 L90,90 L10,90 Z" />
  </svg>
</div>
```

---

## 🎨 Рекомендации по дизайну:

### Цвет логотипа:
Т.к. фон темно-фиолетовый, лучше использовать:
- ✅ Белый
- ✅ Светло-фиолетовый (#b39ddb, #ce93d8)
- ✅ Светло-розовый
- ❌ Темные цвета (плохо видно)

### Формат:
- **PNG**: С прозрачным фоном
- **SVG**: Векторный (лучший вариант)
- Размер файла: < 100KB

### Размеры:
- Минимум: 200x200px
- Рекомендуется: 512x512px
- Соотношение: квадрат (1:1)

---

## 📍 Где находится код для замены:

Откройте файл:
```
frontend/app/page.tsx
```

Найдите строки **~90-102** (поиск по тексту):
```tsx
{/* Премиум иконка вместо эмодзи */}
<div className="mx-auto w-16 h-16 sm:w-20 sm:h-20 relative">
```

Замените весь этот блок на один из вариантов выше.

---

## 🔧 Полный код замены:

### Открываем `frontend/app/page.tsx`

**Было:**
```tsx
{/* Премиум иконка вместо эмодзи */}
<div className="mx-auto w-16 h-16 sm:w-20 sm:h-20 relative">
  <div className="absolute inset-0 bg-gradient-to-br from-purple-400 to-purple-600 rounded-2xl rotate-45 opacity-80"></div>
  <div className="absolute inset-[2px] bg-gradient-to-tl from-purple-950 to-black rounded-2xl rotate-45"></div>
  <div className="absolute inset-0 flex items-center justify-center">
    <svg className="w-8 h-8 sm:w-10 sm:h-10 text-purple-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M13 10V3L4 14h7v7l9-11h-7z" />
    </svg>
  </div>
</div>
```

**Стало (простой вариант):**
```tsx
{/* Ваш логотип */}
<div className="mx-auto mb-6">
  <img 
    src="/logo.png" 
    alt="Геометрия" 
    className="w-24 h-24 sm:w-32 sm:h-32 object-contain"
  />
</div>
```

**Стало (с эффектами):**
```tsx
{/* Ваш логотип с свечением */}
<div className="mx-auto mb-6 relative">
  <div className="absolute inset-0 bg-purple-500/20 blur-3xl rounded-full"></div>
  <img 
    src="/logo.png" 
    alt="Геометрия" 
    className="relative w-24 h-24 sm:w-32 sm:h-32 object-contain filter drop-shadow-2xl"
  />
</div>
```

---

## 🖼️ Пример структуры файлов:

```
frontend/
├── public/
│   ├── logo.png          ← Ваш логотип
│   ├── logo-white.png    ← Белая версия (опционально)
│   ├── favicon.ico
│   └── ...
├── app/
│   ├── page.tsx          ← Здесь меняем код
│   └── ...
```

---

## 🎯 Частые вопросы:

### Q: Логотип не отображается?
A: Проверьте:
1. Файл находится в `frontend/public/`
2. Имя файла правильное (с расширением)
3. Путь в коде: `/logo.png` (со слешем в начале)
4. Обновите страницу (F5)

### Q: Логотип слишком большой/маленький?
A: Измените классы размера:
```tsx
// Маленький
className="w-16 h-16 sm:w-20 sm:h-20"

// Средний (рекомендуется)
className="w-24 h-24 sm:w-32 sm:h-32"

// Большой
className="w-32 h-32 sm:w-40 sm:h-40"
```

### Q: Логотип растянут?
A: Добавьте `object-contain`:
```tsx
className="... object-contain"
```

### Q: Хочу круглый логотип?
A: Добавьте `rounded-full`:
```tsx
className="... rounded-full"
```

---

## 🎨 Примеры кастомизации:

### Круглый логотип с рамкой:
```tsx
<div className="mx-auto mb-6">
  <div className="w-32 h-32 sm:w-40 sm:h-40 rounded-full border-4 border-purple-400/30 p-2 bg-white/5 backdrop-blur-sm">
    <img 
      src="/logo.png" 
      alt="Геометрия" 
      className="w-full h-full object-contain rounded-full"
    />
  </div>
</div>
```

### С анимацией при загрузке:
```tsx
<div className="mx-auto mb-6 animate-fade-in">
  <img 
    src="/logo.png" 
    alt="Геометрия" 
    className="w-24 h-24 sm:w-32 sm:h-32 object-contain hover:scale-110 transition-transform duration-300"
  />
</div>
```

### С градиентным фоном:
```tsx
<div className="mx-auto mb-6">
  <div className="relative w-32 h-32 sm:w-40 sm:h-40 rounded-2xl bg-gradient-to-br from-purple-500/20 to-pink-500/20 backdrop-blur-sm border border-white/10 flex items-center justify-center">
    <img 
      src="/logo.png" 
      alt="Геометрия" 
      className="w-20 h-20 sm:w-28 sm:h-28 object-contain"
    />
  </div>
</div>
```

---

## ✅ Чеклист:

- [ ] Подготовил логотип (PNG/SVG)
- [ ] Положил в `frontend/public/logo.png`
- [ ] Открыл `frontend/app/page.tsx`
- [ ] Нашел блок с текущей иконкой (~строка 90)
- [ ] Заменил на код с `<img src="/logo.png" .../>`
- [ ] Сохранил файл
- [ ] Обновил страницу (F5)
- [ ] Логотип отображается! ✨

---

## 🆘 Если нужна помощь:

1. Отправьте мне логотип - я подскажу оптимальный вариант
2. Скажите какой эффект хотите (с рамкой, свечением и т.д.)
3. Я создам готовый код специально под ваш логотип!

---

**Готово! Теперь вы знаете как добавить свой логотип! 🎨✨**

