# 🚀 Миграция данных из SQLite в Supabase

## 📋 Обзор

Этот гайд поможет вам перенести все данные из SQLite базы данных в Supabase PostgreSQL.

## 🔧 Шаг 1: Создание таблиц в Supabase

### Вариант A: Через SQL Editor (Рекомендуется)

1. Откройте ваш проект в Supabase Dashboard
2. Перейдите в **SQL Editor**
3. Создайте новый запрос
4. Скопируйте содержимое файла `supabase/migrations/001_initial_schema.sql`
5. Вставьте в SQL Editor
6. Нажмите **Run** (или F5)

### Вариант B: Через командную строку

Если у вас установлен Supabase CLI:

```bash
supabase db push
```

## 📦 Шаг 2: Установка зависимостей для миграции

В папке `backend/` установите необходимые пакеты:

```bash
cd backend
npm install @supabase/supabase-js dotenv
```

## 🔐 Шаг 3: Настройка переменных окружения

Создайте или обновите файл `backend/.env`:

```env
# Supabase Configuration
SUPABASE_URL=https://njcsizoiirqfsrzvlzec.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Или используйте существующие переменные из frontend
NEXT_PUBLIC_SUPABASE_URL=https://njcsizoiirqfsrzvlzec.supabase.co
```

**Важно:** Для миграции нужен **Service Role Key**, а не Anon Key!

### Как получить Service Role Key:

1. Откройте Supabase Dashboard
2. Перейдите в **Settings** → **API**
3. Найдите **service_role key** (секретный ключ)
4. Скопируйте его в `.env`

⚠️ **ВНИМАНИЕ:** Service Role Key имеет полный доступ к базе данных. Никогда не коммитьте его в Git!

## 🚀 Шаг 4: Запуск миграции данных

После создания таблиц и настройки переменных окружения:

```bash
cd backend
node scripts/migrateToSupabase.js
```

Скрипт автоматически:
- ✅ Подключится к SQLite базе данных
- ✅ Подключится к Supabase
- ✅ Мигрирует все таблицы по порядку:
  1. Пользователи (users)
  2. Залы (halls)
  3. Направления (directions)
  4. Тренеры (trainers)
  5. Типы абонементов (subscription_types)
  6. Абонементы (subscriptions)
  7. Занятия (lessons)
  8. Записи на занятия (bookings)
  9. Аренда (rental_bookings)
  10. Уведомления (notifications)
  11. Настройки (settings)

## ✅ Шаг 5: Проверка миграции

После завершения миграции проверьте данные в Supabase:

1. Откройте **Table Editor** в Supabase Dashboard
2. Проверьте каждую таблицу на наличие данных
3. Сравните количество записей с SQLite

### Проверка через SQL:

```sql
-- Проверка количества записей в каждой таблице
SELECT 'users' as table_name, COUNT(*) as count FROM users
UNION ALL
SELECT 'halls', COUNT(*) FROM halls
UNION ALL
SELECT 'directions', COUNT(*) FROM directions
UNION ALL
SELECT 'trainers', COUNT(*) FROM trainers
UNION ALL
SELECT 'subscription_types', COUNT(*) FROM subscription_types
UNION ALL
SELECT 'subscriptions', COUNT(*) FROM subscriptions
UNION ALL
SELECT 'lessons', COUNT(*) FROM lessons
UNION ALL
SELECT 'bookings', COUNT(*) FROM bookings
UNION ALL
SELECT 'rental_bookings', COUNT(*) FROM rental_bookings
UNION ALL
SELECT 'notifications', COUNT(*) FROM notifications
UNION ALL
SELECT 'settings', COUNT(*) FROM settings;
```

## 🔄 Шаг 6: Настройка синхронизации (опционально)

Если вы хотите, чтобы данные автоматически синхронизировались между SQLite и Supabase, можно:

1. **Использовать Supabase как основную БД** - обновить backend для работы с Supabase
2. **Двусторонняя синхронизация** - создать middleware для синхронизации изменений
3. **Только чтение из Supabase** - использовать Supabase для аналитики и отчетов

## 🐛 Решение проблем

### Ошибка: "relation does not exist"

**Решение:** Убедитесь, что вы выполнили SQL скрипт создания таблиц в Supabase.

### Ошибка: "permission denied"

**Решение:** 
- Убедитесь, что используете **Service Role Key**, а не Anon Key
- Проверьте политики Row Level Security в Supabase

### Ошибка: "duplicate key value"

**Решение:** Скрипт использует `upsert`, поэтому можно запускать его несколько раз. Данные будут обновляться, а не дублироваться.

### Ошибка подключения к SQLite

**Решение:** 
- Убедитесь, что файл `backend/dev.db` существует
- Проверьте права доступа к файлу

## 📊 Структура миграции

```
supabase/
└── migrations/
    └── 001_initial_schema.sql    # SQL скрипт создания таблиц

backend/
└── scripts/
    └── migrateToSupabase.js      # Скрипт миграции данных
```

## ✅ После миграции

1. ✅ Все данные перенесены в Supabase
2. ✅ Таблицы созданы с правильными индексами
3. ✅ Row Level Security настроен
4. ✅ Внешние ключи установлены

Теперь вы можете использовать Supabase для:
- Real-time подписок
- Хранения файлов
- Расширенной аналитики
- И многого другого!

---

**Готово! Данные успешно мигрированы в Supabase! 🎉**

