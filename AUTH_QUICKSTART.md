# 🚀 Быстрый старт: Аутентификация через Telegram

## ✅ Что уже настроено

1. **Backend middleware** - валидация initData от Telegram
2. **Frontend AuthProvider** - автоматическая инициализация при загрузке
3. **API интеграция** - автоматическое добавление токена в запросы
4. **Хуки для защиты страниц** - `useRequireAuth` и `useRequireAdmin`

## 📝 Как использовать

### 1. Автоматическая авторизация

Авторизация происходит автоматически при загрузке приложения. Ничего дополнительного делать не нужно!

### 2. Использование в компонентах

```tsx
'use client'

import { useAuth } from '@/components/AuthProvider'

export default function MyPage() {
  const { user, isLoading, isAuthenticated } = useAuth()

  if (isLoading) return <div>Загрузка...</div>
  if (!isAuthenticated) return <div>Необходима авторизация</div>

  return <div>Привет, {user?.firstName}!</div>
}
```

### 3. Защита страниц

```tsx
'use client'

import { useRequireAuth } from '@/hooks/useRequireAuth'

export default function ProtectedPage() {
  const { user } = useRequireAuth() // Автоматически перенаправит, если не авторизован

  return <div>Защищенная страница</div>
}
```

### 4. Админские страницы

```tsx
'use client'

import { useRequireAdmin } from '@/hooks/useRequireAuth'

export default function AdminPage() {
  const { user } = useRequireAdmin() // Проверит и авторизацию, и права админа

  return <div>Админ-панель</div>
}
```

## 🔧 Проверка работы

### В браузере (консоль)

```javascript
// Проверить наличие Telegram WebApp
console.log(window.Telegram?.WebApp)

// Проверить initData
console.log(window.Telegram?.WebApp?.initData)

// Проверить токен
console.log(localStorage.getItem('token'))

// Проверить пользователя
console.log(JSON.parse(localStorage.getItem('user') || 'null'))
```

### На сервере (логи)

При успешной авторизации вы увидите:
```
✅ Telegram auth успешна для пользователя 123456789 (Иван)
✅ Пользователь вошел: Иван (ID: 1)
```

При ошибках:
```
❌ Отсутствует hash в initData
❌ Невалидный hash в initData
❌ initData устарел
```

## 🐛 Решение проблем

### Проблема: "Telegram WebApp не доступен"

**Решение:**
- Убедитесь, что открываете приложение через Telegram
- Проверьте, что скрипт `telegram-web-app.js` загружен (см. `layout.tsx`)

### Проблема: "Invalid Telegram Web App data"

**Решение:**
- Проверьте `BOT_TOKEN` в `.env` файле backend
- Убедитесь, что токен совпадает с токеном бота в BotFather

### Проблема: "initData устарел"

**Решение:**
- Обновите страницу в Telegram
- initData действителен только 1 час

### Проблема: Токен не сохраняется

**Решение:**
- Проверьте, что localStorage доступен
- Проверьте консоль на ошибки JavaScript

## 📚 Дополнительная информация

Полная документация: [AUTHENTICATION.md](./AUTHENTICATION.md)

