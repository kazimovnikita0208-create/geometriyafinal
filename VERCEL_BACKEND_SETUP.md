# 🚀 Настройка Backend для работы с Supabase на Vercel

## 📋 Текущая ситуация

У вас уже добавлены переменные для **Frontend**:
- ✅ `NEXT_PUBLIC_API_URL`
- ✅ `NEXT_PUBLIC_SUPABASE_URL`
- ✅ `NEXT_PUBLIC_SUPABASE_ANON_KEY`

## ⚠️ Что нужно добавить для Backend

Если backend деплоится на Vercel, добавьте эти переменные:

### Обязательные переменные:

1. **SUPABASE_URL**
   - Значение: `https://njcsizoiirqfsrzvlzec.supabase.co`
   - Для: Backend (чтобы адаптер знал, что использовать Supabase)

2. **SUPABASE_SERVICE_ROLE_KEY**
   - Значение: ваш service_role key из Supabase
   - Где взять: Supabase Dashboard → Settings → API → service_role key (секретный)
   - ⚠️ Важно: Это секретный ключ, не anon key!

3. **BOT_TOKEN**
   - Значение: ваш Telegram Bot Token
   - Где взять: [@BotFather](https://t.me/BotFather) в Telegram

4. **JWT_SECRET**
   - Значение: секретный ключ для JWT (минимум 32 символа)
   - Можно сгенерировать: `openssl rand -base64 32`
   - Или использовать существующий из `backend/.env`

5. **ADMIN_TELEGRAM_IDS**
   - Значение: список Telegram ID админов через запятую
   - Пример: `123456789,987654321`
   - Где узнать ID: [@userinfobot](https://t.me/userinfobot)

6. **NODE_ENV**
   - Значение: `production`

## 🔧 Как добавить переменные в Vercel

1. В Vercel Dashboard откройте ваш проект
2. Перейдите в **Settings** → **Environment Variables**
3. Нажмите **Add New**
4. Добавьте каждую переменную:
   - **Key**: название переменной (например, `SUPABASE_URL`)
   - **Value**: значение переменной
   - **Environment**: выберите `Production`, `Preview`, `Development` (или все)
5. Нажмите **Save**

## ✅ После добавления переменных

1. **Пересоберите проект:**
   - Перейдите в **Deployments**
   - Нажмите **Redeploy** на последнем деплое
   - Или сделайте новый commit - Vercel автоматически пересоберёт

2. **Проверьте работу:**
   - Backend автоматически переключится на Supabase
   - Все данные будут браться из Supabase вместо SQLite

## 🔍 Как проверить, что всё работает

После деплоя проверьте логи в Vercel:
- Должно появиться: `📦 Используется Supabase`
- Не должно быть ошибок подключения к БД

## 📝 Важно

- **Service Role Key** - секретный ключ, не коммитьте его в Git
- Все переменные должны быть добавлены в Vercel Dashboard
- После добавления переменных нужно пересобрать проект

---

**Добавьте эти переменные в Vercel, и backend будет работать с Supabase! 🎉**

