# 📦 Как распаковать ngrok.exe в папку проекта

Пошаговая инструкция для Windows.

---

## 🎯 Шаг 1: Скачайте ngrok

1. Откройте браузер
2. Перейдите на: **https://ngrok.com/download**
3. Найдите раздел **"Windows"**
4. Нажмите кнопку **"Download"** или **"Download for Windows"**
5. Файл скачается (обычно в папку `Загрузки`)

Файл будет называться примерно так: `ngrok-v3-stable-windows-amd64.zip`

---

## 📂 Шаг 2: Найдите скачанный файл

1. Откройте **Проводник Windows** (Win + E)
2. Перейдите в папку **Загрузки** (Downloads)
3. Найдите файл `ngrok-v3-stable-windows-amd64.zip` (или похожий)

---

## 📁 Шаг 3: Откройте папку проекта

1. В том же Проводнике перейдите в:
   ```
   C:\Users\Nikita\Desktop\Geomrtriya
   ```

Или просто откройте папку проекта на рабочем столе.

---

## 🔓 Шаг 4: Распакуйте ngrok

### Способ А: Через Проводник (самый простой)

1. **Щелкните правой кнопкой** на файле `ngrok-v3-stable-windows-amd64.zip`
2. Выберите **"Извлечь все..."** или **"Extract All..."**
3. В окне "Извлечь сжатые файлы":
   - Нажмите **"Обзор..."** или **"Browse..."**
   - Выберите папку: `C:\Users\Nikita\Desktop\Geomrtriya`
   - Нажмите **"Выбрать папку"**
   - Снимите галочку **"Показать извлеченные файлы"** (не обязательно)
   - Нажмите **"Извлечь"**

4. После распаковки в папке `Geomrtriya` должен появиться файл **`ngrok.exe`**

### Способ Б: Перетаскиванием

1. Откройте ZIP файл двойным кликом (откроется как папка)
2. Найдите внутри файл **`ngrok.exe`**
3. **Перетащите** `ngrok.exe` в папку `Geomrtriya` (на рабочем столе)

### Способ В: Через PowerShell

1. Откройте PowerShell в папке Загрузки:
   ```powershell
   cd $HOME\Downloads
   ```

2. Распакуйте в папку проекта:
   ```powershell
   Expand-Archive -Path "ngrok-v3-stable-windows-amd64.zip" -DestinationPath "C:\Users\Nikita\Desktop\Geomrtriya" -Force
   ```

---

## ✅ Шаг 5: Проверьте результат

1. Откройте папку `C:\Users\Nikita\Desktop\Geomrtriya`
2. Убедитесь, что там есть файл **`ngrok.exe`**

Вы должны увидеть что-то вроде:
```
Geomrtriya/
├── backend/
├── frontend/
├── ngrok.exe          ← Вот он!
├── README.md
└── ...
```

---

## 🚀 Шаг 6: Проверьте работу

Откройте PowerShell в папке проекта и выполните:

```powershell
cd C:\Users\Nikita\Desktop\Geomrtriya
.\ngrok.exe version
```

Если всё правильно, вы увидите версию ngrok, например:
```
ngrok version 3.x.x
```

---

## 🎯 Итоговая структура

После распаковки структура должна быть такой:

```
C:\Users\Nikita\Desktop\Geomrtriya\
│
├── ngrok.exe              ← ngrok здесь!
│
├── backend\
│   └── ...
│
├── frontend\
│   └── ...
│
├── start-ngrok.bat       ← скрипт для запуска
│
└── ...
```

---

## ⚠️ Важно!

- ✅ Файл должен называться именно **`ngrok.exe`** (не `ngrok-v3.exe` или другое)
- ✅ Файл должен быть в корне папки `Geomrtriya` (не во вложенных папках)
- ✅ Если в ZIP было несколько файлов, нужен только `ngrok.exe`

---

## 🐛 Если что-то пошло не так

### Проблема: "Файл не распаковывается"
→ Убедитесь, что ZIP файл не повреждён. Попробуйте скачать заново.

### Проблема: "ngrok.exe не запускается"
→ Возможно, Windows блокирует файл. Щелкните правой кнопкой → Свойства → Разблокировать → ОК

### Проблема: "Файл в другой папке"
→ Просто перетащите `ngrok.exe` в папку `Geomrtriya` вручную

---

## 📝 После распаковки

1. Настройте токен (один раз):
   ```powershell
   .\ngrok.exe config add-authtoken 343zW38rrAoaE7YzWO7fmW2gnhP_5rdqW39qekFhMxBc9C8xU
   ```

2. Запустите ngrok:
   ```powershell
   .\ngrok.exe http 3002
   ```

Или используйте готовый скрипт:
```powershell
.\start-ngrok.bat
```

---

**Готово! ngrok.exe распакован и готов к использованию! 🎉**

