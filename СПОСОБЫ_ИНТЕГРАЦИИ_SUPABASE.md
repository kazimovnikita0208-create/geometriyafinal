# 🔗 Способы интеграции приложения с Supabase

## 📋 Текущая интеграция

### ✅ Что уже используется:

1. **REST API через Supabase Client** (Frontend)
   - Прямые запросы к таблицам через `@supabase/supabase-js`
   - Используется для синхронизации пользователей

2. **REST API через Backend Adapter** (Backend)
   - Адаптер базы данных переключается между SQLite и Supabase
   - Используется для всех операций с БД

## 🚀 Дополнительные способы интеграции

### 1. **Supabase Realtime** (Real-time обновления)

**Что это:** Автоматические обновления данных в реальном времени без перезагрузки страницы.

**Применение:**
- Обновление расписания занятий в реальном времени
- Уведомления о новых бронированиях
- Live обновления статистики для админов

**Пример использования:**

```typescript
// frontend/lib/realtime.ts
import { createClient } from '@/utils/supabase/client'

export function subscribeToBookings(callback: (booking: any) => void) {
  const supabase = createClient()
  
  const channel = supabase
    .channel('bookings-changes')
    .on(
      'postgres_changes',
      {
        event: '*', // INSERT, UPDATE, DELETE
        schema: 'public',
        table: 'bookings'
      },
      (payload) => {
        callback(payload.new)
      }
    )
    .subscribe()

  return () => {
    supabase.removeChannel(channel)
  }
}

// Использование в компоненте
useEffect(() => {
  const unsubscribe = subscribeToBookings((booking) => {
    // Обновить список бронирований
    setBookings(prev => [...prev, booking])
  })
  
  return unsubscribe
}, [])
```

**Преимущества:**
- ✅ Мгновенные обновления без перезагрузки
- ✅ Меньше нагрузки на сервер (не нужно polling)
- ✅ Лучший UX

---

### 2. **Supabase Storage** (Хранение файлов)

**Что это:** Облачное хранилище для файлов (изображения, документы, видео).

**Применение:**
- Загрузка аватаров пользователей
- Фото тренеров
- Документы абонементов
- Видео с занятий

**Пример использования:**

```typescript
// frontend/lib/storage.ts
import { createClient } from '@/utils/supabase/client'

export async function uploadAvatar(userId: string, file: File) {
  const supabase = createClient()
  
  const { data, error } = await supabase.storage
    .from('avatars')
    .upload(`${userId}/${file.name}`, file, {
      cacheControl: '3600',
      upsert: true
    })

  if (error) throw error

  // Получить публичный URL
  const { data: { publicUrl } } = supabase.storage
    .from('avatars')
    .getPublicUrl(data.path)

  return publicUrl
}

// Загрузка файла
const handleUpload = async (file: File) => {
  try {
    const url = await uploadAvatar(user.id, file)
    // Обновить профиль пользователя с новым URL аватара
  } catch (error) {
    console.error('Ошибка загрузки:', error)
  }
}
```

**Настройка в Supabase:**
1. Supabase Dashboard → Storage
2. Создайте bucket (например, `avatars`)
3. Настройте политики доступа (Public или Private)

**Преимущества:**
- ✅ CDN для быстрой загрузки
- ✅ Автоматическая оптимизация изображений
- ✅ Безопасное хранение файлов

---

### 3. **Supabase Edge Functions** (Serverless функции)

**Что это:** Серверные функции, которые выполняются на edge-серверах Supabase.

**Применение:**
- Обработка платежей
- Отправка email уведомлений
- Сложные вычисления
- Интеграции с внешними API

**Пример использования:**

```typescript
// supabase/functions/send-notification/index.ts
import { serve } from 'https://deno.land/std@0.168.0/http/server.ts'
import { createClient } from 'https://esm.sh/@supabase/supabase-js@2'

serve(async (req) => {
  const { userId, message } = await req.json()
  
  const supabase = createClient(
    Deno.env.get('SUPABASE_URL') ?? '',
    Deno.env.get('SUPABASE_SERVICE_ROLE_KEY') ?? ''
  )

  // Отправить уведомление пользователю
  const { data } = await supabase
    .from('notifications')
    .insert({
      user_id: userId,
      message: message,
      created_at: new Date().toISOString()
    })

  return new Response(JSON.stringify({ success: true }), {
    headers: { 'Content-Type': 'application/json' },
  })
})

// Вызов из frontend
const response = await fetch(
  'https://njcsizoiirqfsrzvlzec.supabase.co/functions/v1/send-notification',
  {
    method: 'POST',
    headers: {
      'Authorization': `Bearer ${supabaseAnonKey}`,
      'Content-Type': 'application/json',
    },
    body: JSON.stringify({ userId: user.id, message: 'Новое занятие!' })
  }
)
```

**Преимущества:**
- ✅ Масштабируемость
- ✅ Нет необходимости в отдельном сервере
- ✅ Быстрое выполнение

---

### 4. **Row Level Security (RLS)** (Безопасность данных)

**Что это:** Политики безопасности на уровне строк, которые контролируют доступ к данным.

**Применение:**
- Пользователи видят только свои бронирования
- Админы имеют доступ ко всем данным
- Ограничение доступа к личным данным

**Пример настройки:**

```sql
-- В Supabase SQL Editor

-- Политика: пользователи видят только свои бронирования
CREATE POLICY "Users can view own bookings"
ON bookings
FOR SELECT
USING (auth.uid()::text = user_id::text);

-- Политика: пользователи могут создавать свои бронирования
CREATE POLICY "Users can insert own bookings"
ON bookings
FOR INSERT
WITH CHECK (auth.uid()::text = user_id::text);

-- Политика: админы видят все бронирования
CREATE POLICY "Admins can view all bookings"
ON bookings
FOR SELECT
USING (
  EXISTS (
    SELECT 1 FROM users
    WHERE users.id = auth.uid()::integer
    AND users.is_admin = true
  )
);
```

**Преимущества:**
- ✅ Безопасность на уровне БД
- ✅ Защита от SQL инъекций
- ✅ Контроль доступа без дополнительного кода

---

### 5. **Supabase Triggers** (Автоматизация)

**Что это:** Автоматические действия при изменении данных в БД.

**Применение:**
- Автоматическое создание уведомлений
- Обновление статистики
- Валидация данных
- Логирование изменений

**Пример настройки:**

```sql
-- Триггер: автоматическое создание уведомления при новом бронировании
CREATE OR REPLACE FUNCTION notify_new_booking()
RETURNS TRIGGER AS $$
BEGIN
  INSERT INTO notifications (user_id, message, type, created_at)
  VALUES (
    NEW.user_id,
    'Вы записались на занятие!',
    'booking_created',
    NOW()
  );
  RETURN NEW;
END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER on_booking_created
AFTER INSERT ON bookings
FOR EACH ROW
EXECUTE FUNCTION notify_new_booking();
```

**Преимущества:**
- ✅ Автоматизация без кода
- ✅ Надежность (работает на уровне БД)
- ✅ Производительность

---

### 6. **Supabase Webhooks** (Интеграции)

**Что это:** HTTP запросы, которые отправляются при событиях в БД.

**Применение:**
- Интеграция с внешними сервисами
- Отправка email через внешний сервис
- Синхронизация с другими системами

**Настройка:**
1. Supabase Dashboard → Database → Webhooks
2. Создайте webhook для таблицы (например, `bookings`)
3. Укажите URL вашего сервера
4. Выберите события (INSERT, UPDATE, DELETE)

**Преимущества:**
- ✅ Интеграция с любыми сервисами
- ✅ Асинхронная обработка
- ✅ Масштабируемость

---

### 7. **Supabase Auth** (Аутентификация)

**Что это:** Встроенная система аутентификации Supabase (хотя у вас своя через Telegram).

**Применение:**
- Альтернативная авторизация (email/password)
- OAuth (Google, GitHub и т.д.)
- Magic links

**Пример (если нужно добавить):**

```typescript
// Альтернативная авторизация через email
const { data, error } = await supabase.auth.signInWithPassword({
  email: 'user@example.com',
  password: 'password'
})

// OAuth авторизация
const { data, error } = await supabase.auth.signInWithOAuth({
  provider: 'google',
  options: {
    redirectTo: 'https://yourapp.com/callback'
  }
})
```

**Преимущества:**
- ✅ Готовая система аутентификации
- ✅ Поддержка OAuth
- ✅ Управление сессиями

---

### 8. **Supabase CLI** (Миграции и управление)

**Что это:** Командная строка для управления Supabase проектом.

**Применение:**
- Локальная разработка с Supabase
- Миграции БД
- Тестирование

**Установка и использование:**

```bash
# Установка
npm install -g supabase

# Логин
supabase login

# Инициализация проекта
supabase init

# Создание миграции
supabase migration new add_new_table

# Применение миграций
supabase db push

# Локальный запуск
supabase start
```

**Преимущества:**
- ✅ Версионирование БД
- ✅ Локальная разработка
- ✅ Автоматизация деплоя

---

### 9. **Supabase Studio** (Визуальное управление)

**Что это:** Веб-интерфейс для управления данными и настройками.

**Применение:**
- Просмотр и редактирование данных
- Настройка таблиц
- Управление пользователями
- Просмотр логов

**Доступ:**
- Supabase Dashboard → Table Editor
- Supabase Dashboard → SQL Editor
- Supabase Dashboard → API Docs

**Преимущества:**
- ✅ Удобный интерфейс
- ✅ Не требует знания SQL
- ✅ Быстрый доступ к данным

---

### 10. **Прямые SQL запросы** (Расширенные возможности)

**Что это:** Выполнение SQL запросов напрямую через Supabase.

**Применение:**
- Сложные запросы с JOIN
- Агрегация данных
- Массовые обновления

**Пример:**

```typescript
// Выполнение SQL запроса
const { data, error } = await supabase.rpc('get_user_statistics', {
  user_id: userId
})

// Или через SQL Editor в Supabase Dashboard
```

**Преимущества:**
- ✅ Полный контроль над запросами
- ✅ Оптимизация производительности
- ✅ Сложная логика на уровне БД

---

## 🎯 Рекомендации для вашего проекта

### Приоритет 1 (Рекомендуется добавить):
1. **Supabase Realtime** - для live обновлений расписания и бронирований
2. **Row Level Security** - для безопасности данных пользователей
3. **Supabase Triggers** - для автоматических уведомлений

### Приоритет 2 (Полезно, но не обязательно):
4. **Supabase Storage** - если нужна загрузка файлов
5. **Supabase Edge Functions** - для сложной серверной логики

### Приоритет 3 (Опционально):
6. **Supabase Webhooks** - для интеграций
7. **Supabase Auth** - если нужна альтернативная авторизация

---

## 📚 Документация

- [Supabase Realtime](https://supabase.com/docs/guides/realtime)
- [Supabase Storage](https://supabase.com/docs/guides/storage)
- [Supabase Edge Functions](https://supabase.com/docs/guides/functions)
- [Row Level Security](https://supabase.com/docs/guides/auth/row-level-security)
- [Supabase Triggers](https://supabase.com/docs/guides/database/triggers)

---

**Какой способ интеграции вас больше всего интересует? Могу помочь с реализацией! 🚀**

