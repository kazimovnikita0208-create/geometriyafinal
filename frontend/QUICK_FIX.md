# 🔧 Быстрое исправление проблем

## Проблема: Страница /prices не открывается или показывает ошибку

### Решение 1: Перезапуск Frontend

1. **Остановите frontend** (нажмите `Ctrl+C` в терминале где запущен `npm run dev`)

2. **Очистите кэш Next.js:**
   ```bash
   cd frontend
   .\restart-clean.bat
   ```

3. **Или вручную:**
   ```bash
   cd frontend
   rm -r .next
   rm -r node_modules\.cache
   npm run dev
   ```

### Решение 2: Проверка серверов

Убедитесь, что оба сервера запущены:

**Backend (порт 3001):**
```bash
cd backend
node src/index.js
```

Должно появиться:
```
✅ Подключение к базе данных успешно
🚀 РЕЖИМ РАЗРАБОТКИ АКТИВЕН - авторизация отключена
🚀 Сервер запущен!
📡 API: http://localhost:3001
```

**Frontend (порт 3002):**
```bash
cd frontend
npm run dev
```

Должно появиться:
```
- ready started server on 0.0.0.0:3002, url: http://localhost:3002
- event compiled client and server successfully
```

### Решение 3: Проверка API

Откройте в браузере:
```
http://localhost:3001/api/subscription-types
```

Должны увидеть JSON с абонементами.

### Решение 4: Проверка консоли браузера

1. Откройте DevTools (F12)
2. Перейдите на вкладку Console
3. Обновите страницу
4. Посмотрите на ошибки (красные сообщения)

**Частые ошибки:**

- **"Failed to fetch"** → Backend не запущен или неправильный URL
- **"Syntax error"** → Ошибка компиляции TypeScript/JSX
- **404 на странице** → Frontend не скомпилировался

### Решение 5: Полный перезапуск

1. **Остановите все:**
   ```powershell
   Get-Process -Name node | Stop-Process -Force
   ```

2. **Backend:**
   ```bash
   cd backend
   node src/index.js
   ```

3. **Frontend (в новом терминале):**
   ```bash
   cd frontend
   npm run dev
   ```

4. **Подождите 10-15 секунд** для компиляции

5. **Откройте:**
   ```
   http://localhost:3002/prices
   ```

## Что вы должны видеть на /prices:

1. ✅ Фиолетовый анимированный фон
2. ✅ Заголовок "Абонементы"
3. ✅ Категории абонементов:
   - КЛАССИЧЕСКИЙ
   - ТОЛЬКО ФИТНЕС
   - КОМБО-АБОНЕМЕНТ
4. ✅ Карточки с ценами и кнопками "Начать заниматься"

## Если ничего не помогло:

Опишите, что именно вы видите:
- Белый экран?
- Ошибка с текстом?
- Страница загружается, но без стилей?
- Только фон без контента?

Также проверьте консоль браузера (F12 → Console) и пришлите текст ошибок.

