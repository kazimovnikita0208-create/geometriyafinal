# 🔗 Настройка подключения Frontend к Backend

## Шаг 1: Создайте файл `.env.local`

В папке `frontend/` создайте файл `.env.local` со следующим содержимым:

```env
# Backend API URL
NEXT_PUBLIC_API_URL=http://localhost:3001

# Telegram Bot Username (замените на ваш username бота)
NEXT_PUBLIC_BOT_USERNAME=your_bot_username
```

## Шаг 2: Установите зависимости (если еще не установлены)

```bash
cd frontend
npm install
```

## Шаг 3: Запустите frontend

```bash
npm run dev
```

Frontend будет доступен на `http://localhost:3000`

## 🎯 Что уже работает

### ✅ Интегрированные страницы:

1. **Направления** (`/directions`)
   - Загружает список направлений из backend
   - Показывает: название, описание, особенности, уровни
   - API: `GET /api/directions`

2. **Абонементы** (`/prices`)
   - Загружает типы абонементов по категориям
   - Показывает: цена, количество занятий, срок действия
   - API: `GET /api/subscription-types`

### 🔄 Как это работает

Все API запросы идут через `/lib/api.ts`:

```typescript
import { directionsAPI, subscriptionTypesAPI } from '@/lib/api'

// Загрузка направлений
const response = await directionsAPI.getAll()

// Загрузка абонементов
const response = await subscriptionTypesAPI.getAll()
```

## 🚀 Запуск полного приложения

### Терминал 1 - Backend:
```bash
cd backend
npm run dev
```

### Терминал 2 - Frontend:
```bash
cd frontend
npm run dev
```

### Откройте браузер:
- Frontend: http://localhost:3000
- Backend API: http://localhost:3001/api

## 🔍 Проверка работы

1. Откройте http://localhost:3000
2. Перейдите на страницу "О направлениях"
3. Внизу страницы должно быть: "✅ Данные загружены из backend (6 направлений)"
4. Перейдите на страницу "Абонемент"
5. Внизу должно быть: "✅ Данные загружены из backend (15 абонементов)"

## 📊 API Endpoints

Все доступные endpoints:

- `GET /api/directions` - список направлений
- `GET /api/directions/:slug` - одно направление
- `GET /api/halls` - список залов
- `GET /api/subscription-types` - типы абонементов
- `POST /api/auth/login` - аутентификация через Telegram
- `GET /api/auth/me` - текущий пользователь

## 🛠️ Разработка

Файл `/lib/api.ts` содержит:
- `directionsAPI` - методы для работы с направлениями
- `hallsAPI` - методы для работы с залами
- `subscriptionTypesAPI` - методы для работы с абонементами
- `authAPI` - методы для аутентификации

Добавляйте новые методы по мере необходимости!

## ⚠️ Возможные проблемы

### Ошибка CORS
Если видите ошибку CORS, убедитесь что:
1. Backend запущен на порту 3001
2. В backend `.env` указан `FRONTEND_URL=http://localhost:3000`

### Ошибка подключения
Если видите "Не удалось загрузить данные":
1. Проверьте что backend запущен (`npm run dev` в папке backend)
2. Проверьте что в `.env.local` указан правильный URL: `NEXT_PUBLIC_API_URL=http://localhost:3001`
3. Откройте http://localhost:3001/api в браузере - должен вернуться JSON

## 🎉 Готово!

Frontend и Backend успешно связаны! Данные теперь загружаются из базы данных SQLite через API.

