# 🔧 Исправление ошибки сборки на Vercel

## ❌ Ошибка

```
Module not found: Can't resolve '@/components/ui/beams-background'
Module not found: Can't resolve '@/components/ui/button'
Module not found: Can't resolve '@/lib/api'
```

## ✅ Решение

### Вариант 1: Установить Root Directory в Vercel (Рекомендуется)

1. Откройте Vercel Dashboard → ваш проект
2. Перейдите в **Settings** → **General**
3. Найдите раздел **Root Directory**
4. Установите: `frontend`
5. Сохраните

После этого `vercel.json` в корне не нужен (или должен быть простым).

### Вариант 2: Использовать vercel.json в корне

Если Root Directory не установлен, используйте обновленный `vercel.json` (уже исправлен).

## 🔍 Проверка

После настройки:
1. Перейдите в **Deployments**
2. Нажмите **Redeploy**
3. Проверьте логи - ошибки должны исчезнуть

## ⚠️ Важно

- Если Root Directory = `frontend`, то команды в vercel.json должны быть без `cd frontend`
- Если Root Directory не установлен, то нужны команды с `cd frontend`

---

**Установите Root Directory = `frontend` в Vercel Dashboard!**

