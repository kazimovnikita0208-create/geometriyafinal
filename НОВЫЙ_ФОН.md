# ✨ Добавлен анимированный фон BeamsBackground

## 🎨 Что изменилось:

### Новый красивый фон с анимированными лучами!

**Особенности:**
- 💜 **Фиолетовые анимированные лучи** (цвет студии #5833b6)
- ✨ **Плавная анимация** на Canvas
- 🌟 **Пульсирующие эффекты**
- 🎭 **Градиентный фон** от фиолетового до чёрного
- 📱 **Адаптивный** под любые экраны

---

## 📦 Что было установлено:

### Новые зависимости:
- ✅ `framer-motion` - для плавных анимаций
- ✅ `clsx` - для работы с CSS классами
- ✅ `tailwind-merge` - для объединения Tailwind классов

### Новые файлы:
```
frontend/
├── components/
│   └── ui/
│       └── beams-background.tsx  ← Компонент с анимированным фоном
├── lib/
│   └── utils.ts                  ← Утилита cn для классов
└── app/
    └── page.tsx                  ← Обновлена главная страница
```

---

## 🎯 Как это работает:

### BeamsBackground компонент:
- Создаёт Canvas элемент на весь экран
- Рисует 30 анимированных "лучей"
- Каждый луч движется с разной скоростью
- Лучи имеют эффект пульсации (меняют яркость)
- Используется blur для мягкого эффекта

### Технические детали:
```typescript
// Параметры компонента
intensity: "subtle" | "medium" | "strong"  // Интенсивность эффекта
className: string                           // Дополнительные стили
children: ReactNode                         // Контент поверх фона
```

### Цвета:
- **Hue (оттенок)**: 270-300 (фиолетовый диапазон)
- **Saturation**: 85% (насыщенность)
- **Lightness**: 65% (яркость)
- **Фон**: gradient от purple-950 → black → purple-900

---

## 📱 Откройте и посмотрите!

### **http://localhost:3000**

Вы увидите:
- 💜 Анимированные фиолетовые лучи на фоне
- ✨ Плавное движение и пульсацию
- 🎭 Все кнопки теперь поверх красивого фона
- 🌟 Эффект глубины и объёма

---

## 🎛️ Настройки интенсивности:

В файле `app/page.tsx` можно изменить:

```tsx
<BeamsBackground intensity="medium">  // ← Измените на:
// "subtle"  - Слабый эффект (50% прозрачность)
// "medium"  - Средний эффект (70% прозрачность) ✅ Текущий
// "strong"  - Сильный эффект (100% прозрачность)
```

---

## 🔧 Кастомизация:

### Изменить цвет лучей:
В файле `components/ui/beams-background.tsx` найдите:

```typescript
// Строка 32 и 102
hue: 270 + Math.random() * 30,  // Фиолетовый (270-300)

// Измените на другие цвета:
// 0-30    - Красный
// 60-90   - Жёлтый
// 120-150 - Зелёный
// 180-210 - Голубой
// 240-270 - Синий
// 270-300 - Фиолетовый ✅ Текущий
// 300-330 - Пурпурный
```

### Изменить количество лучей:
```typescript
// Строка 65
const MINIMUM_BEAMS = 20;  // Увеличьте для большего количества
```

### Изменить скорость анимации:
```typescript
// Строка 42
speed: 0.6 + Math.random() * 1.2,  // Увеличьте цифры для ускорения
```

---

## 🎨 Совместимость:

✅ Работает со всеми страницами
✅ Адаптивный дизайн
✅ Оптимизирован для производительности
✅ Использует requestAnimationFrame для плавной анимации
✅ Автоматически масштабируется под retina дисплеи

---

## 🚀 Производительность:

- **Canvas** - аппаратное ускорение GPU
- **blur(35px)** - для мягкого эффекта
- **~30 лучей** - оптимальный баланс красота/производительность
- **60 FPS** - плавная анимация

---

## 💡 Использование на других страницах:

Чтобы добавить фон на другие страницы:

```tsx
import { BeamsBackground } from '@/components/ui/beams-background'

export default function MyPage() {
  return (
    <BeamsBackground intensity="medium">
      <div>
        {/* Ваш контент здесь */}
      </div>
    </BeamsBackground>
  )
}
```

---

**Наслаждайтесь красивым анимированным фоном! ✨💜**

