# 🚀 Деплой на Vercel - Решение проблемы 404

## Проблема
После деплоя на Vercel появляется ошибка **404: NOT_FOUND**.

## ✅ Решение

### Вариант 1: Настройка Root Directory в Vercel (Рекомендуется)

1. Откройте проект на Vercel: https://vercel.com/dashboard
2. Выберите ваш проект `geometriyafinal`
3. Перейдите в **Settings** → **General**
4. Найдите раздел **Root Directory**
5. Нажмите **Edit**
6. Введите: `frontend`
7. Нажмите **Save**

Теперь Vercel будет использовать папку `frontend` как корневую директорию проекта.

### Вариант 2: Использование vercel.json

Файл `vercel.json` уже создан в корне проекта. Он указывает Vercel:
- Собирать проект из папки `frontend`
- Использовать `.next` как output directory

### Вариант 3: Деплой только frontend папки

Если предыдущие варианты не работают:

1. В Vercel Dashboard → Settings → General
2. Установите:
   - **Root Directory**: `frontend`
   - **Build Command**: `npm run build`
   - **Output Directory**: `.next`
   - **Install Command**: `npm install`

---

## 🔧 Настройка переменных окружения

В Vercel Dashboard → Settings → Environment Variables добавьте:

```
NEXT_PUBLIC_API_URL=https://your-backend-url.com
```

Или для локального тестирования:
```
NEXT_PUBLIC_API_URL=http://localhost:3001
```

---

## 📝 После настройки

1. Перейдите в **Deployments**
2. Нажмите **Redeploy** на последнем деплое
3. Или сделайте новый commit и push - Vercel автоматически пересоберёт проект

---

## ✅ Проверка

После пересборки откройте ваш сайт:
```
https://geometriyafinal.vercel.app
```

Должна открыться главная страница приложения "Геометрия"!

---

## 🐛 Если всё ещё 404

1. Проверьте логи деплоя в Vercel Dashboard
2. Убедитесь, что сборка прошла успешно
3. Проверьте, что файл `frontend/app/page.tsx` существует
4. Убедитесь, что `Root Directory` установлен в `frontend`

---

**После настройки Root Directory всё должно заработать! 🎉**

