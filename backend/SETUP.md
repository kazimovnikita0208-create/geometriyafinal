# 🚀 Инструкция по развертыванию Backend

## Шаг 1: Установка зависимостей

```bash
cd backend
npm install
```

## Шаг 2: Настройка переменных окружения

Создайте файл `.env` в папке `backend/`:

```env
# Server
PORT=3001
NODE_ENV=development

# Database
DATABASE_URL="file:./dev.db"

# JWT
JWT_SECRET=your_super_secret_jwt_key_here_change_in_production_geometriya_2024
JWT_EXPIRES_IN=7d

# Telegram Bot
BOT_TOKEN=your_telegram_bot_token_here
TELEGRAM_BOT_USERNAME=your_bot_username

# Admin (замените на ваши Telegram ID через запятую)
ADMIN_TELEGRAM_IDS=123456789,987654321

# Frontend
FRONTEND_URL=http://localhost:3000

# Notifications
REMINDER_HOURS_BEFORE=4
CANCEL_EVENING_HOURS_BEFORE=4
CANCEL_MORNING_DEADLINE_HOUR=21
SUBSCRIPTION_EXPIRY_WARNING_DAYS=3
```

### Как получить Telegram Bot Token:

1. Откройте [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям (придумайте имя и username для бота)
4. Скопируйте полученный токен в `.env` файл
5. Настройте Web App командой `/newapp`

### Как узнать свой Telegram ID:

1. Откройте [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте команду `/start`
3. Бот покажет ваш ID
4. Добавьте его в `ADMIN_TELEGRAM_IDS` в `.env`

## Шаг 3: Инициализация базы данных

```bash
# Создать таблицы (миграция)
npx prisma migrate dev --name init

# Заполнить базу начальными данными
npx prisma db seed
```

## Шаг 4: Запуск сервера

```bash
# Development режим (с автоперезагрузкой)
npm run dev

# Production режим
npm start
```

Сервер будет доступен на `http://localhost:3001`

## Шаг 5: Проверка работы

Откройте в браузере или Postman:

```
GET http://localhost:3001/health
```

Должен вернуться ответ:
```json
{
  "status": "ok",
  "timestamp": "2024-11-22T...",
  "environment": "development"
}
```

## Полезные команды

```bash
# Посмотреть базу данных через GUI
npx prisma studio

# Пересоздать базу данных
npx prisma migrate reset

# Создать новую миграцию
npx prisma migrate dev --name <название>

# Обновить Prisma Client после изменений схемы
npx prisma generate

# Заполнить базу тестовыми данными
npx prisma db seed
```

## Структура базы данных

После инициализации будут созданы следующие данные:

- **2 зала** (Волгина, 117А и ТОЦ "Охотный ряд")
- **6 направлений** (Pole Fit, Pole Exotic, Сила & Гибкость, Растяжка, Choreo, Strip)
- **3 тренера** (Анна Иванова, Мария Петрова, Ольга Смирнова)
- **15 типов абонементов** (3 категории)
- **9 настроек системы**

## Возможные проблемы

### Ошибка "JWT_SECRET не установлен"
Убедитесь, что в `.env` файле есть переменная `JWT_SECRET`

### Ошибка "BOT_TOKEN не найден"
Получите токен у @BotFather и добавьте в `.env`

### Ошибка при миграции
Удалите папку `prisma/migrations` и файл `dev.db`, затем выполните миграцию заново

### Port already in use
Измените `PORT` в `.env` на другой (например, 3002)

## Следующие шаги

После успешного запуска backend:

1. Протестируйте API endpoints
2. Настройте интеграцию с frontend
3. Настройте Telegram Web App
4. Добавьте первого админа (ваш Telegram ID)

---

**Готово!** 🎉 Backend запущен и готов к работе!


