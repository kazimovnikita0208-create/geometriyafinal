# 🔧 Установка ngrok на Windows

## Проблема
Ошибка: `ngrok : Имя "ngrok" не распознано`

Это означает, что ngrok не установлен или не добавлен в PATH.

---

## ✅ Решение: Установка ngrok

### Способ 1: Через Chocolatey (рекомендуется)

Если у вас установлен Chocolatey:

```powershell
choco install ngrok
```

### Способ 2: Ручная установка

#### Шаг 1: Скачайте ngrok

1. Откройте: https://ngrok.com/download
2. Выберите **Windows** версию
3. Скачайте ZIP файл

#### Шаг 2: Распакуйте

1. Распакуйте `ngrok.exe` в удобную папку, например:
   ```
   C:\ngrok\ngrok.exe
   ```
   или в папку проекта:
   ```
   C:\Users\Nikita\Desktop\Geomrtriya\ngrok.exe
   ```

#### Шаг 3: Добавьте в PATH (опционально)

**Вариант А: Использовать напрямую из папки**

Просто запускайте ngrok с полным путём:
```powershell
C:\ngrok\ngrok.exe http 3002
```

Или если в папке проекта:
```powershell
.\ngrok.exe http 3002
```

**Вариант Б: Добавить в PATH (для удобства)**

1. Скопируйте путь к папке с ngrok.exe (например: `C:\ngrok`)
2. Откройте: Панель управления → Система → Дополнительные параметры системы
3. Нажмите "Переменные среды"
4. В "Системные переменные" найдите `Path` → "Изменить"
5. Нажмите "Создать" → вставьте путь к папке с ngrok
6. Нажмите "ОК" везде
7. **Перезапустите PowerShell** (важно!)

---

## 🔑 Настройка authtoken

После установки ngrok, настройте ваш токен:

```powershell
ngrok config add-authtoken 343zW38rrAoaE7YzWO7fmW2gnhP_5rdqW39qekFhMxBc9C8xU
```

Если ngrok не в PATH, используйте полный путь:
```powershell
C:\ngrok\ngrok.exe config add-authtoken 343zW38rrAoaE7YzWO7fmW2gnhP_5rdqW39qekFhMxBc9C8xU
```

---

## 🚀 Запуск ngrok

### Если ngrok в PATH:
```powershell
ngrok http 3002
```

### Если ngrok в папке проекта:
```powershell
.\ngrok.exe http 3002
```

### Если ngrok в другой папке:
```powershell
C:\ngrok\ngrok.exe http 3002
```

---

## ✅ Проверка

После запуска вы должны увидеть:

```
ngrok

Session Status                online
Account                       ваш_email (Plan: Free)
Version                       3.x.x
Region                        United States (us)
Latency                       -
Web Interface                 http://127.0.0.1:4040
Forwarding                    https://abc123.ngrok-free.app -> http://localhost:3002

Connections                   ttl     opn     rt1     rt5     p50     p90
                              0       0       0.00    0.00    0.00    0.00
```

**Скопируйте HTTPS URL:** `https://abc123.ngrok-free.app`

---

## 🎯 Быстрый способ (без установки)

Если не хотите устанавливать, можно использовать ngrok напрямую:

1. Скачайте `ngrok.exe` в папку проекта
2. Запускайте так:
   ```powershell
   .\ngrok.exe http 3002
   ```

---

## 📝 Создание скрипта для удобства

Создайте файл `start-ngrok.bat` в корне проекта:

```batch
@echo off
echo Запуск ngrok для порта 3002...
ngrok http 3002
```

Или если ngrok в папке проекта:
```batch
@echo off
echo Запуск ngrok для порта 3002...
.\ngrok.exe http 3002
```

Теперь просто запускайте: `start-ngrok.bat`

---

## 🐛 Решение проблем

### "ngrok.exe не найден"
→ Убедитесь, что файл `ngrok.exe` находится в указанной папке

### "Access denied"
→ Запустите PowerShell от имени администратора

### "Authtoken не работает"
→ Убедитесь, что токен скопирован полностью, без пробелов

---

**После установки ngrok всё должно работать! 🎉**

